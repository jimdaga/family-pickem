---
phase: 04-family-scoped-app-pages
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/urls.py
  - pickem/pickem_homepage/forms.py
  - pickem/pickem_homepage/templates/pickem/picks.html
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements:
  - AUTHZ-02
  - AUTHZ-05
  - POOL-03
  - SEC-03
user_setup: []
must_haves:
  truths:
    - Pick submit and edit routes live under `/families/<family_slug>/pools/<pool_slug>/...` and require active membership, per D-01 and D-04.
    - Legacy signed-in `/picks/` and `/picks/edit/` redirect into the user's resolved tenant context and do not render or mutate global private picks, per D-02 and D-03.
    - Pick reads, existing-pick state, lock/edit state, and write targets are current-pool scoped, per D-06.
    - Pick writes derive user, pool, season, week, game, and correctness-related fields server-side, per D-07.
  artifacts:
    - pickem/pickem_homepage/views.py
    - pickem/pickem_homepage/urls.py
    - pickem/pickem_homepage/forms.py
    - pickem/pickem_homepage/templates/pickem/picks.html
    - pickem/pickem_homepage/tests.py
  key_links:
    - Tenant pick views use `request.tenant_context.pool` for all `GamePicks` queries.
    - POST payload supplies only user-selectable pick/tiebreaker values; server derives all ownership and game fields.
    - Pick edit lookup includes current pool and current user before checking lock state.
---

<objective>
Move pick submission and edit flows into tenant URLs with server-derived ownership and pool-scoped reads.

Purpose: Pick writes are the highest-risk gameplay mutation surface in Phase 4.
Output: Tenant pick routes, narrow pick input handling, scoped pick edit endpoint, and negative tests for ID/body tampering.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-family-scoped-app-pages/04-CONTEXT.md
@.planning/phases/04-family-scoped-app-pages/04-RESEARCH.md
@.planning/phases/04-family-scoped-app-pages/04-PATTERNS.md
@.planning/phases/01-domain-schema-foundation/01-02-SUMMARY.md
@.planning/phases/02-authorization-foundation/02-01-SUMMARY.md
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/urls.py
@pickem/pickem_homepage/forms.py
@pickem/pickem_homepage/templates/pickem/picks.html
@pickem/pickem_homepage/tests.py
@pickem/pickem_api/models.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add tenant pick submit and edit tests</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <read_first>pickem/pickem_homepage/tests.py, pickem/pickem_homepage/views.py, .planning/phases/04-family-scoped-app-pages/04-PATTERNS.md</read_first>
  <action>Add tests for D-01 through D-07 and D-17 covering tenant GET picks, tenant POST submit, tenant AJAX edit, legacy signed-in redirects, outsider denial, cross-pool pick ID denial, and POST body tampering with user/pool/season/week/game/correctness fields. Build two families/pools with games in the same season/week; prove a family A member cannot submit into or edit family B pick data by changing URL slugs, `pick_id`, hidden fields, or request body values.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tests fail against the current broad `GamePicksForm` write path.
    - Tests assert saved `GamePicks.pool`, user identifiers, game fields, and `pick_correct` are server-derived.
    - Tests assert legacy signed-in pick routes redirect before rendering private global picks.
  </acceptance_criteria>
  <done>Pick isolation and server-derived write behavior are covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tenant pick routes and server-derived writes</name>
  <files>pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_homepage/forms.py</files>
  <read_first>pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_homepage/forms.py, pickem/pickem_api/models.py, .planning/phases/04-family-scoped-app-pages/04-RESEARCH.md</read_first>
  <action>Add tenant URL names for pick submission and pick edit under the explicit family/pool path per D-01. Protect tenant views with `family_member_required` per D-04. Refactor pick GET queries to keep `GamesAndScores`, `GameWeeks`, and `Teams` global per D-05 while filtering `GamePicks` by current `tenant_context.pool` and current user per D-06. Replace tenant POST usage of broad model-form ownership fields with narrow validation of selected game/team and tiebreakers; derive `pool`, `userEmail`, `uid`, `userID`, `slug`, `competition`, `gameseason`, `gameWeek`, `gameyear`, `pick_game_id`, and `pick_correct` server-side from current user, current pool, and the resolved game per D-07. Legacy signed-in pick routes should redirect to the matching tenant route when a default tenant is resolvable per D-02 and D-03.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tenant pick save creates or updates only a current-pool/current-user pick for the resolved game.
    - Client-provided ownership, season/week/game, and correctness fields are ignored or rejected.
    - Pick edit uses current-pool/current-user lookup before validating lock state and selected team.
  </acceptance_criteria>
  <done>Pick submit/edit are tenant-routed, membership-guarded, and server-derived.</done>
</task>

<task type="auto">
  <name>Task 3: Update picks template for tenant actions</name>
  <files>pickem/pickem_homepage/templates/pickem/picks.html</files>
  <read_first>pickem/pickem_homepage/templates/pickem/picks.html, .planning/phases/04-family-scoped-app-pages/04-PATTERNS.md</read_first>
  <action>Update the picks template so form actions, AJAX edit URLs, post-submit links, and empty states preserve current `family.slug` and `pool.slug` per D-21 and D-22. Remove or stop rendering hidden server-owned pick fields on tenant pages; keep only selected team and tiebreaker inputs as user-editable data per D-07. Preserve current styling conventions without broad visual migration work, and keep Phase 3 header/mobile context visible per D-24.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tenant pick form posts to the tenant route.
    - Edit controls call the tenant edit endpoint.
    - No rendered tenant pick form trusts family, pool, user, game, week, season, or correctness hidden fields.
  </acceptance_criteria>
  <done>Picks UI submits and edits inside the current tenant context.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->pick POST | User-controlled form/AJAX payload requests pick creation or mutation. |
| pick view->database | View resolves global games and writes pool-owned `GamePicks`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | tenant pick submit/edit | critical | mitigate | Derive all ownership and game fields server-side; validate selected team against resolved game; test body tampering. |
| T-04-04 | Information Disclosure | pick read/edit lookup | high | mitigate | Filter pick lookup by current pool and current user before returning state or errors. |
| T-04-SC | Tampering | package installs | high | mitigate | No package installs are planned; reject any install attempt unless a Package Legitimacy Audit is added first. |
</threat_model>

<verification>
- [ ] `cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2`
- [ ] `cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings`
</verification>

<success_criteria>
- Tenant picks can be submitted and edited only by active members in the current pool.
- Legacy signed-in pick routes redirect instead of rendering or mutating global private picks.
- Cross-family pick URL, ID, query, and body tampering tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-family-scoped-app-pages/04-02-SUMMARY.md`.
</output>
