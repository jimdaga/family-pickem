---
phase: 04-family-scoped-app-pages
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - pickem/pickem_homepage/templates/pickem/base.html
  - pickem/pickem_homepage/templates/pickem/home.html
  - pickem/pickem_homepage/templates/pickem/picks.html
  - pickem/pickem_homepage/templates/pickem/scores.html
  - pickem/pickem/context_processors.py
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements:
  - AUTHZ-04
  - SEC-03
  - SEC-04
user_setup: []
must_haves:
  truths:
    - Header, mobile navigation, dashboard, picks, and scores links preserve explicit family/pool context, per D-21 and D-24.
    - Empty states stay inside the current family/pool context and do not send signed-in users to global private app pages, per D-22.
    - Shared navigation, dashboard, picks, and scores templates no longer send signed-in tenant users to legacy global private pages.
    - Shared context processors do not leak global private rank, current-week pick, standing, or banner data into tenant pages; footer stats are scoped to `current_pool` or suppressed.
  artifacts:
    - pickem/pickem_homepage/templates/pickem/base.html
    - pickem/pickem_homepage/templates/pickem/home.html
    - pickem/pickem_homepage/templates/pickem/picks.html
    - pickem/pickem_homepage/templates/pickem/scores.html
    - pickem/pickem/context_processors.py
    - pickem/pickem_homepage/tests.py
  key_links:
    - Shared navigation builds tenant URLs from current server-derived family/pool context.
    - Dashboard, picks, and scores calls to action use tenant route names after their owning routes exist.
    - `footer_stats_context` must include pool scope for private stats or return no private stats.
    - `site_banner_context` must not render another family's banner on tenant pages.
---

<objective>
Clean up shared navigation, shared context processors, dashboard, picks, and scores tenant links.

Purpose: Tenant-scoped data can still leak UX context if links route signed-in users back to legacy private pages.
Output: Shared header/mobile navigation cleanup, tenant-safe shared context processors, and tenant link cleanup for dashboard, picks, and scores.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-family-scoped-app-pages/04-CONTEXT.md
@.planning/phases/04-family-scoped-app-pages/04-RESEARCH.md
@.planning/phases/04-family-scoped-app-pages/04-PATTERNS.md
@.planning/phases/03-onboarding-and-family-selection/03-04-SUMMARY.md
@.planning/phases/03-onboarding-and-family-selection/03-05-SUMMARY.md
@pickem/pickem_homepage/templates/pickem/base.html
@pickem/pickem_homepage/templates/pickem/home.html
@pickem/pickem_homepage/templates/pickem/picks.html
@pickem/pickem_homepage/templates/pickem/scores.html
@pickem/pickem/context_processors.py
@pickem/pickem_homepage/tests.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add shared link and context leakage regression tests</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <read_first>pickem/pickem_homepage/tests.py, pickem/pickem/context_processors.py, .planning/phases/04-family-scoped-app-pages/04-CONTEXT.md, .planning/phases/04-family-scoped-app-pages/04-RESEARCH.md, .planning/phases/04-family-scoped-app-pages/04-PATTERNS.md</read_first>
  <action>Add regression tests for D-21, D-22, D-24, and SEC-04 that render shared header/mobile navigation, tenant dashboard, tenant picks, and tenant scores as a logged-in member. Assert private navigation/form/AJAX/profile links include `/families/<family_slug>/pools/<pool_slug>/` instead of legacy global private paths. Add `Phase4SharedContextScopeTests` or equivalent coverage proving `footer_stats_context` rank/current-week pick stats are scoped to `current_pool` or suppressed, and `site_banner_context` cannot show a family B banner on a family A tenant page. Preserve cross-family negative assertions already added by earlier plans and focus this task on link/context regressions for the files this plan modifies.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tests fail if shared navigation, tenant dashboard, tenant picks, or tenant scores link to legacy global private app routes.
    - Tests preserve anonymous public routes where already public-safe.
    - Tests fail if shared footer stats use another pool's rows or if another family's banner appears on a tenant page.
  </acceptance_criteria>
  <done>Shared/dashboard/picks/scores tenant link regressions and shared context leakage regressions are covered.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up shared header links and context processors</name>
  <files>pickem/pickem_homepage/templates/pickem/base.html, pickem/pickem/context_processors.py</files>
  <read_first>pickem/pickem_homepage/templates/pickem/base.html, pickem/pickem_homepage/templates/pickem/home.html, pickem/pickem/context_processors.py, .planning/phases/04-family-scoped-app-pages/04-PATTERNS.md</read_first>
  <action>Update shared desktop/mobile header links, dropdown week links, profile menu links, and context-derived URLs so signed-in tenant pages preserve current family/pool context per D-21. Update `footer_stats_context` so private rank/current-week pick stats include `pool=current_pool` whenever tenant context exists, and suppress private footer stats rather than using global values when a tenant pool cannot be safely resolved on private pages. Update `site_banner_context` so tenant pages prefer current-family active banners, then safe site-wide banners if supported by the model, and never select another family's banner. Keep Phase 3 current family/pool switcher visible per D-24. Keep public/anonymous links public-safe per D-04 and do not alter commissioner/admin editing surfaces.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Mobile and desktop navigation preserve current family/pool context.
    - Header switcher choices remain active-membership-only.
    - Footer private stats are pool-scoped or suppressed.
    - Tenant pages do not render another family's banner.
  </acceptance_criteria>
  <done>Shared navigation and shared context processors preserve tenant context.</done>
</task>

<task type="auto">
  <name>Task 3: Clean up dashboard picks and scores tenant links</name>
  <files>pickem/pickem_homepage/templates/pickem/home.html, pickem/pickem_homepage/templates/pickem/picks.html, pickem/pickem_homepage/templates/pickem/scores.html</files>
  <read_first>pickem/pickem_homepage/templates/pickem/home.html, pickem/pickem_homepage/templates/pickem/picks.html, pickem/pickem_homepage/templates/pickem/scores.html, .planning/phases/04-family-scoped-app-pages/04-PATTERNS.md</read_first>
  <action>Update dashboard calls to action, picks completion links, pick form/edit endpoint references, score week links, score profile links, and score picks links so signed-in tenant pages preserve current family/pool context per D-21. Empty states should point to current tenant destinations or remain local to the page per D-22. Keep public/anonymous behavior public-safe per D-04.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Dashboard, picks, and scores private links preserve current family/pool context.
    - Empty states do not link signed-in tenant users to global private app pages.
  </acceptance_criteria>
  <done>Dashboard, picks, and scores templates preserve tenant context.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template navigation->tenant routes | Server-rendered links decide whether users stay in scoped pages or fall back to global private routes. |
| template links->tenant routes | Dashboard, picks, and scores links must stay inside current tenant context. |
| context processors->tenant pages | Shared footer/banner context can inject private or family-specific data into otherwise scoped pages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Information Disclosure | shared navigation and empty states | medium | mitigate | Assert migrated tenant pages do not link to legacy global private surfaces. |
| T-04-12 | Information Disclosure | dashboard/picks/scores links | medium | mitigate | Assert modified tenant templates do not route signed-in users to global private pages. |
| T-04-13 | Information Disclosure | shared footer stats and banners | high | mitigate | Scope private footer stats by current pool or suppress them, and constrain tenant banners to current family/site-wide records only. |
| T-04-SC | Tampering | package installs | high | mitigate | No package installs are planned; reject any install attempt unless a Package Legitimacy Audit is added first. |
</threat_model>

<verification>
- [ ] `cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings`
- [ ] `cd pickem && ../venv/bin/python manage.py makemigrations --check --dry-run --settings=pickem.test_settings`
- [ ] `cd pickem && ../venv/bin/python manage.py test pickem_homepage pickem_api --settings=pickem.test_settings --verbosity=2`
- [ ] `cd pickem && ../venv/bin/python manage.py test --settings=pickem.test_settings --verbosity=2`
- [ ] `curl -s --max-time 5 http://localhost:8000 | head -40`
</verification>

<success_criteria>
- Shared navigation preserves explicit family/pool URLs for signed-in tenant pages.
- Dashboard, picks, and scores links/forms/AJAX stay inside the current tenant context.
- Shared context processors do not leak global/private stats or another family's banner into tenant pages.
- Anonymous public-safe links continue to render.
</success_criteria>

<output>
After completion, create `.planning/phases/04-family-scoped-app-pages/04-05-SUMMARY.md`.
</output>
