---
phase: 04-family-scoped-app-pages
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/urls.py
  - pickem/pickem_homepage/templates/pickem/scores.html
  - pickem/pickem_homepage/templates/pickem/standings.html
  - pickem/pickem_homepage/templates/pickem/rules.html
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements:
  - AUTHZ-04
  - POOL-03
  - POOL-04
  - SEC-03
  - SEC-04
user_setup: []
must_haves:
  truths:
    - Scores routes use global NFL games/weeks/teams while all picks, pick totals, player overlays, and weekly stats are current-pool scoped, per D-05, D-06, and D-08.
    - Standings and weekly winners read from current pool scope and legacy signed-in access redirects into tenant URLs, per D-11.
    - Rules are tenant-aware and display current family/pool rules/settings without editing controls, per D-18, D-19, and D-20.
    - Derived standings and score overlay data avoid unscoped caches/precomputed reads, per D-12.
  artifacts:
    - pickem/pickem_homepage/views.py
    - pickem/pickem_homepage/urls.py
    - pickem/pickem_homepage/templates/pickem/scores.html
    - pickem/pickem_homepage/templates/pickem/standings.html
    - pickem/pickem_homepage/templates/pickem/rules.html
    - pickem/pickem_homepage/tests.py
  key_links:
    - Tenant score overlay querysets include `pool=tenant_context.pool`.
    - Tenant standings querysets include `pool=tenant_context.pool`.
    - Rules template receives current `family`, `pool`, and optional `pool.settings`.
---

<objective>
Migrate scores, standings, weekly winners, and rules into tenant context while preserving global NFL reference facts.

Purpose: Scores and standings are high-visibility read surfaces where global reference data and private pool overlays must be separated cleanly.
Output: Tenant score/standings/rules routes, scoped querysets, tenant links, and cross-family negative tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-family-scoped-app-pages/04-CONTEXT.md
@.planning/phases/04-family-scoped-app-pages/04-RESEARCH.md
@.planning/phases/04-family-scoped-app-pages/04-PATTERNS.md
@.planning/phases/01-domain-schema-foundation/01-02-SUMMARY.md
@.planning/phases/03-onboarding-and-family-selection/03-04-SUMMARY.md
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/urls.py
@pickem/pickem_homepage/templates/pickem/scores.html
@pickem/pickem_homepage/templates/pickem/standings.html
@pickem/pickem_homepage/templates/pickem/rules.html
@pickem/pickem_homepage/tests.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add scores standings and rules isolation tests</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <read_first>pickem/pickem_homepage/tests.py, .planning/phases/04-family-scoped-app-pages/04-PATTERNS.md</read_first>
  <action>Add tests for D-01 through D-06, D-08, D-11, D-12, D-17, D-18, D-19, D-20, D-21, D-22, D-23, and D-24. Cover tenant scores current week and selected week, tenant standings season filter, tenant rules display, legacy signed-in redirects, outsider direct URL denial, and cross-family data exclusion. Prove a member of family A sees shared global game facts but not family B pick totals, player overlays, weekly winner, standings rows, profile links, or settings labels by changing slugs or query params.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tests demonstrate global NFL game facts render for both families.
    - Tests demonstrate private pick overlays, standings, and rules context are tenant-specific.
    - Tests confirm no editing form is present on rules pages.
  </acceptance_criteria>
  <done>Scores, standings, and rules tenant contracts are covered by automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tenant scores and standings</name>
  <files>pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py</files>
  <read_first>pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_api/models.py, .planning/phases/04-family-scoped-app-pages/04-RESEARCH.md</read_first>
  <action>Add tenant URL names for scores current week, scores by competition/season/week, and standings under the explicit family/pool path per D-01. Protect them with `family_member_required` per D-04. Keep `GamesAndScores`, `GameWeeks`, and `Teams` global per D-05. Filter every `GamePicks`, `userSeasonPoints`, user weekly stat, pick count, player overlay, and winner queryset by `tenant_context.pool` per D-06, D-08, D-11, and D-12. Restrict standings player/member lists to active members of `tenant_context.family`. Legacy signed-in scores and standings routes should redirect to matching tenant URLs when a default tenant is resolvable per D-02 and D-03.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tenant scores show global game facts and current-pool-only overlays.
    - Tenant standings and weekly winners read current-pool data only.
    - Legacy signed-in scores and standings do not render private global data.
  </acceptance_criteria>
  <done>Scores and standings are tenant-routed and pool-scoped.</done>
</task>

<task type="auto">
  <name>Task 3: Implement tenant rules and update score standings templates</name>
  <files>pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_homepage/templates/pickem/scores.html, pickem/pickem_homepage/templates/pickem/standings.html, pickem/pickem_homepage/templates/pickem/rules.html</files>
  <read_first>pickem/pickem_homepage/templates/pickem/scores.html, pickem/pickem_homepage/templates/pickem/standings.html, pickem/pickem_homepage/templates/pickem/rules.html, .planning/phases/04-family-scoped-app-pages/04-PATTERNS.md</read_first>
  <action>Add tenant rules route/view protected by tenant membership per D-18. Pass current `family`, `pool`, `membership`, and `pool.settings` when present; use existing static rules copy only as display fallback per D-20. Do not add rules/settings edit forms or owner/admin mutation controls per D-19. Update scores and standings template profile links, week links, picks links, season filters, and empty states to preserve explicit family/pool context per D-21 and D-22 while retaining Phase 3 header/mobile tenant context per D-24.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Scores and standings templates link only to tenant-aware private pages when current tenant context exists.
    - Rules page displays current family/pool context and settings values if available.
    - No rules/settings mutation form is introduced.
  </acceptance_criteria>
  <done>Scores, standings, and rules render tenant-preserving UI.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->tenant read routes | User-controlled slug, season, competition, and week values select tenant page data. |
| tenant read routes->database | Views combine global NFL facts with private pool overlays and standings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Information Disclosure | score overlays | high | mitigate | Keep global facts separate from pool-owned picks and user stats; add cross-family overlay tests. |
| T-04-06 | Information Disclosure | standings and weekly winners | high | mitigate | Filter standings/winners by current pool and members by current family. |
| T-04-07 | Elevation of Privilege | rules page | medium | mitigate | Display settings only; do not introduce mutation controls. |
| T-04-SC | Tampering | package installs | high | mitigate | No package installs are planned; reject any install attempt unless a Package Legitimacy Audit is added first. |
</threat_model>

<verification>
- [ ] `cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2`
- [ ] `cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings`
</verification>

<success_criteria>
- Tenant scores show global NFL facts with current-pool-only overlays.
- Tenant standings and weekly winners are current-pool scoped.
- Tenant rules are display-only and current-family/current-pool aware.
</success_criteria>

<output>
After completion, create `.planning/phases/04-family-scoped-app-pages/04-03-SUMMARY.md`.
</output>
