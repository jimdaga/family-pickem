---
phase: 04-family-scoped-app-pages
plan: 06
type: execute
wave: 6
depends_on:
  - 04-05
files_modified:
  - pickem/pickem_homepage/templates/pickem/standings.html
  - pickem/pickem_homepage/templates/pickem/rules.html
  - pickem/pickem_homepage/templates/pickem/user_profile.html
  - pickem/pickem_homepage/tests.py
  - .planning/phases/04-family-scoped-app-pages/04-VALIDATION.md
autonomous: true
requirements:
  - AUTHZ-02
  - AUTHZ-04
  - AUTHZ-05
  - POOL-03
  - POOL-04
  - COMM-02
  - SEC-03
  - SEC-04
user_setup: []
must_haves:
  truths:
    - Standings, rules, and profile templates preserve explicit family/pool context for private links, per D-21 and D-24.
    - Final negative tests prove cross-family isolation for posts, comments, votes, player list, profile stats, pick data, scores overlays, standings, shared footer stats, family-specific banners, and dashboard data, per D-17.
    - Phase 04 validation maps every requirement and locked decision to plan, implementation evidence, and automated verification.
  artifacts:
    - pickem/pickem_homepage/templates/pickem/standings.html
    - pickem/pickem_homepage/templates/pickem/rules.html
    - pickem/pickem_homepage/templates/pickem/user_profile.html
    - pickem/pickem_homepage/tests.py
    - .planning/phases/04-family-scoped-app-pages/04-VALIDATION.md
  key_links:
    - Final tests exercise direct URL slugs, object IDs, query params, and request bodies across two families.
    - Validation references `04-01-SUMMARY.md` through `04-06-SUMMARY.md`.
---

<objective>
Complete remaining tenant link cleanup, final cross-family negative coverage, and Phase 04 validation handoff.

Purpose: The phase is not complete until remaining tenant templates preserve context and isolation claims are mapped to evidence.
Output: Standings/rules/profile tenant link cleanup, final negative tests, final automated verification, and updated `04-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-family-scoped-app-pages/04-CONTEXT.md
@.planning/phases/04-family-scoped-app-pages/04-RESEARCH.md
@.planning/phases/04-family-scoped-app-pages/04-PATTERNS.md
@.planning/phases/03-onboarding-and-family-selection/03-05-SUMMARY.md
@pickem/pickem_homepage/templates/pickem/standings.html
@pickem/pickem_homepage/templates/pickem/rules.html
@pickem/pickem_homepage/templates/pickem/user_profile.html
@pickem/pickem_homepage/tests.py
@.planning/phases/04-family-scoped-app-pages/04-VALIDATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add final cross-family isolation regression tests</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <read_first>pickem/pickem_homepage/tests.py, .planning/phases/04-family-scoped-app-pages/04-CONTEXT.md, .planning/phases/04-family-scoped-app-pages/04-PATTERNS.md</read_first>
  <action>Add final end-to-end negative assertions for D-17, D-21, D-22, D-23, D-24, and SEC-04 using two families. Cover posts, comments, votes, player list, profile stats, pick data, scores overlays, standings rows, shared footer stats, family-specific banners, dashboard widgets, URL slug tampering, object ID tampering, query param tampering, and request body tampering. Render tenant standings, rules, and profile pages as a logged-in member and assert private links include `/families/<family_slug>/pools/<pool_slug>/` instead of legacy global private paths.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tests fail if any cross-family private object is visible or mutable from another family.
    - Tests fail if standings, rules, or profile pages link to legacy global private app routes.
    - Tests preserve non-leaking not-found/error behavior for outsiders.
  </acceptance_criteria>
  <done>Final cross-family isolation and remaining tenant-link regressions are covered.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up standings rules and profile tenant links</name>
  <files>pickem/pickem_homepage/templates/pickem/standings.html, pickem/pickem_homepage/templates/pickem/rules.html, pickem/pickem_homepage/templates/pickem/user_profile.html</files>
  <read_first>pickem/pickem_homepage/templates/pickem/standings.html, pickem/pickem_homepage/templates/pickem/rules.html, pickem/pickem_homepage/templates/pickem/user_profile.html, .planning/phases/04-family-scoped-app-pages/04-PATTERNS.md</read_first>
  <action>Update standings filters, standings profile links, rules links, profile recent-pick links, profile player/team context links, and empty states so tenant pages preserve current family/pool context per D-21 and D-22. Keep rules display-only per D-19 and D-20. Keep Phase 3 header/mobile tenant context visible per D-24.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Standings, rules, and profile pages preserve current family/pool context in private links.
    - Rules remains display-only.
    - Empty states stay in the current tenant context.
  </acceptance_criteria>
  <done>Remaining tenant templates preserve family/pool context.</done>
</task>

<task type="auto">
  <name>Task 3: Run final verification and update Phase 04 validation</name>
  <files>.planning/phases/04-family-scoped-app-pages/04-VALIDATION.md</files>
  <read_first>.planning/phases/04-family-scoped-app-pages/04-VALIDATION.md, .planning/phases/04-family-scoped-app-pages/04-CONTEXT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>Run final automated verification for Phase 04, then update `04-VALIDATION.md` with actual summary references and command outcomes. The validation must map GOAL, REQ, RESEARCH, and CONTEXT decision coverage for D-01 through D-24, requirements AUTHZ-02, AUTHZ-04, AUTHZ-05, POOL-03, POOL-04, COMM-02, SEC-03, and SEC-04, and the final negative test evidence. Do not mark family admin editing, invite management, role management, cron/scoring job hardening, or production migration hardening complete.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings && ../venv/bin/python manage.py makemigrations --check --dry-run --settings=pickem.test_settings && ../venv/bin/python manage.py test pickem_homepage pickem_api --settings=pickem.test_settings --verbosity=2 && ../venv/bin/python manage.py test --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - `04-VALIDATION.md` contains a Nyquist-style source-to-plan-to-test mapping.
    - Final verification records existing warnings separately from failures.
    - Phase 04 handoff explicitly leaves admin editing and operational hardening unclaimed.
  </acceptance_criteria>
  <done>Phase 04 has final verification evidence and validation coverage mapping.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant templates->private routes | Remaining template links decide whether users stay in scoped pages or fall back to global private routes. |
| final verification->phase handoff | Documentation claims must match implemented and tested behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Information Disclosure | standings/rules/profile links | medium | mitigate | Assert remaining tenant templates do not route signed-in users to global private pages. |
| T-04-14 | Information Disclosure | cross-family regression suite | high | mitigate | Exercise slugs, object IDs, query params, and bodies across two families. |
| T-04-16 | Information Disclosure | shared context regression suite | high | mitigate | Include footer stats and family-specific banners in final two-family negative coverage. |
| T-04-15 | Repudiation | validation artifact | medium | mitigate | Map each requirement/decision to plan and verification evidence with summary references. |
| T-04-SC | Tampering | package installs | high | mitigate | No package installs are planned; reject any install attempt unless a Package Legitimacy Audit is added first. |
</threat_model>

<verification>
- [ ] `cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings`
- [ ] `cd pickem && ../venv/bin/python manage.py makemigrations --check --dry-run --settings=pickem.test_settings`
- [ ] `cd pickem && ../venv/bin/python manage.py test pickem_homepage pickem_api --settings=pickem.test_settings --verbosity=2`
- [ ] `cd pickem && ../venv/bin/python manage.py test --settings=pickem.test_settings --verbosity=2`
- [ ] `curl -s --max-time 5 http://localhost:8000 | head -40`
</verification>

<success_criteria>
- Remaining migrated tenant pages preserve explicit family/pool URLs throughout navigation.
- Cross-family negative tests cover the Phase 04 private data surfaces.
- `04-VALIDATION.md` records source coverage and final verification without claiming out-of-scope work.
</success_criteria>

<output>
After completion, create `.planning/phases/04-family-scoped-app-pages/04-06-SUMMARY.md`.
</output>
