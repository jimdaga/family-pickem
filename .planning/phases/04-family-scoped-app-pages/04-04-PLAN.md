---
phase: 04-family-scoped-app-pages
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/urls.py
  - pickem/pickem_homepage/templates/pickem/home.html
  - pickem/pickem_homepage/templates/pickem/user_profile.html
  - pickem/pickem_homepage/templates/pickem/user_profile_private.html
  - pickem/pickem_homepage/templates/pickem/players.html
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements:
  - AUTHZ-02
  - AUTHZ-04
  - COMM-02
  - SEC-03
  - SEC-04
user_setup: []
must_haves:
  truths:
    - Player lists, profiles, profile stats, posts, comments, and votes are family-private, per D-13.
    - Viewing another user's profile or player stats requires active membership in the current family, per D-14.
    - Profile and stat views use current family/pool scope where applicable, per D-15.
    - Message-board create/comment/vote/read endpoints and JSON serialization filter by current family and deny outsiders, per D-16 and D-17.
  artifacts:
    - pickem/pickem_homepage/views.py
    - pickem/pickem_homepage/urls.py
    - pickem/pickem_homepage/templates/pickem/home.html
    - pickem/pickem_homepage/templates/pickem/user_profile.html
    - pickem/pickem_homepage/templates/pickem/players.html
    - pickem/pickem_homepage/tests.py
  key_links:
    - Profile view verifies current viewer and viewed user are active members of `tenant_context.family`.
    - Profile statistics and recent picks are filtered by `tenant_context.pool`.
    - Message-board object lookups include `family=tenant_context.family` before serialization or mutation.
---

<objective>
Make profiles, player lists, and message-board AJAX family-private under tenant URLs.

Purpose: These pages expose user identity, discussion, votes, and pick history, so they need explicit tenant membership and object-scope checks.
Output: Tenant profile/player routes, scoped profile stats, family-scoped message-board endpoints, and IDOR negative tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-family-scoped-app-pages/04-CONTEXT.md
@.planning/phases/04-family-scoped-app-pages/04-RESEARCH.md
@.planning/phases/04-family-scoped-app-pages/04-PATTERNS.md
@.planning/phases/01-domain-schema-foundation/01-03-SUMMARY.md
@.planning/phases/02-authorization-foundation/02-01-SUMMARY.md
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/urls.py
@pickem/pickem_homepage/models.py
@pickem/pickem_homepage/templates/pickem/home.html
@pickem/pickem_homepage/templates/pickem/user_profile.html
@pickem/pickem_homepage/tests.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add profile players and message-board negative tests</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <read_first>pickem/pickem_homepage/tests.py, pickem/pickem_homepage/models.py, .planning/phases/04-family-scoped-app-pages/04-PATTERNS.md</read_first>
  <action>Add tests for D-13, D-14, D-15, D-16, D-17, D-21, D-22, D-23, and D-24. Cover tenant players list, tenant profile view, profile privacy behavior inside family context, message post create, comment create, post vote, comment vote, comment read serialization, legacy signed-in redirects where applicable, outsider denial, and cross-family ID tampering. Prove a family A member cannot see family B player list entries, profile stats, posts, comments, votes, or pick data by changing URL slugs, object IDs, query params, or JSON/request body values.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tests cover both successful same-family access and denied cross-family access.
    - Tests assert JSON endpoints return generic not-found/error behavior for cross-family IDs without exposing target family names.
    - Tests assert profile stats exclude another pool's picks and standings.
  </acceptance_criteria>
  <done>Family-private profile, players, and message-board behavior is covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tenant profiles and players</name>
  <files>pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_homepage/templates/pickem/user_profile.html, pickem/pickem_homepage/templates/pickem/user_profile_private.html, pickem/pickem_homepage/templates/pickem/players.html</files>
  <read_first>pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_homepage/templates/pickem/user_profile.html, pickem/pickem_homepage/templates/pickem/user_profile_private.html, pickem/pickem_api/models.py</read_first>
  <action>Add tenant profile and tenant players route names under the explicit family/pool path per D-01. Create `pickem/pickem_homepage/templates/pickem/players.html` if no suitable player-list template exists, following the existing profile/home template patterns. Protect tenant profile/player routes with `family_member_required` per D-04. For profile views, resolve the target user only through an active `FamilyMembership` in `tenant_context.family` per D-14 and return not-found semantics for outsiders per D-23. Scope `userSeasonPoints`, `GamePicks`, `userStats`, rankings, recent picks, team-pick stats, and message post counts by current pool or family where applicable per D-13 and D-15. Keep profile privacy behavior, but apply it after tenant membership is proven. Legacy signed-in global profile/player access should redirect to tenant context when resolvable or deny private data rendering per D-02 and D-03.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tenant profile never reads another family's membership, picks, standings, stats, posts, or votes.
    - Tenant players list contains only active current-family members.
    - Profile links preserve current family/pool context.
  </acceptance_criteria>
  <done>Profiles and players are tenant-routed and family/private-data scoped.</done>
</task>

<task type="auto">
  <name>Task 3: Implement tenant message-board AJAX</name>
  <files>pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_homepage/templates/pickem/home.html</files>
  <read_first>pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_homepage/templates/pickem/home.html, pickem/pickem_homepage/models.py, .planning/phases/04-family-scoped-app-pages/04-RESEARCH.md</read_first>
  <action>Add tenant message-board AJAX route names for create post, create comment, vote post, vote comment, and read comments under the explicit family/pool path per D-01. Protect session-authenticated message-board write/read endpoints with `family_member_required` per D-04 and derive `family=tenant_context.family` on posts, comments, and votes per D-16. Fetch posts/comments/votes through scoped lookups that include `family=tenant_context.family`; verify parent comments belong to the same scoped post before creating replies. Update homepage/dashboard JavaScript endpoints to use tenant URLs from context rather than hardcoded global `/message-board/...` paths per D-21. Return generic JSON failures for cross-family IDs per D-23.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Creating posts/comments/votes assigns the current family server-side.
    - Cross-family post/comment/vote IDs are not serialized or mutated.
    - Tenant dashboard message-board JavaScript posts to tenant endpoints only.
  </acceptance_criteria>
  <done>Message-board AJAX is family-scoped and tenant-linked.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->profile/player routes | User-controlled tenant slugs and user IDs request private member data. |
| browser->message AJAX | User-controlled JSON/body IDs request post, comment, and vote mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Information Disclosure | tenant profile/player views | high | mitigate | Resolve target users through active current-family membership and scope stats by pool/family. |
| T-04-09 | Tampering | message-board AJAX endpoints | high | mitigate | Guard routes, derive family server-side, and use scoped object lookups before mutation. |
| T-04-10 | Information Disclosure | message-board JSON serialization | high | mitigate | Fetch comments through family-scoped post lookup and return generic cross-family failures. |
| T-04-SC | Tampering | package installs | high | mitigate | No package installs are planned; reject any install attempt unless a Package Legitimacy Audit is added first. |
</threat_model>

<verification>
- [ ] `cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2`
- [ ] `cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings`
</verification>

<success_criteria>
- Tenant profiles and players are visible only to active family members.
- Profile stats and pick history are current-pool/current-family scoped.
- Message-board read/write/vote AJAX cannot cross family boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/04-family-scoped-app-pages/04-04-SUMMARY.md`.
</output>
