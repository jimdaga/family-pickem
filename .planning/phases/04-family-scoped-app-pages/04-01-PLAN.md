---
phase: 04-family-scoped-app-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/urls.py
  - pickem/pickem_homepage/templates/pickem/family_pool_home.html
  - pickem/pickem_homepage/templates/pickem/home.html
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements:
  - AUTHZ-04
  - POOL-03
  - SEC-04
user_setup: []
must_haves:
  truths:
    - Signed-in root and legacy home access routes into an explicit family/pool URL before any private global dashboard query runs, per D-01, D-02, and D-03.
    - Tenant dashboard/home displays current family/default-pool standings, weekly winners, pick status, family message preview, and member activity only from the current pool/family, per D-09 and D-10.
    - Anonymous public home behavior remains available only for public-safe content, per D-04.
    - Dashboard-derived standings, pick counts, message previews, and rank lookups include pool or family scope or are omitted, per D-12.
  artifacts:
    - pickem/pickem_homepage/views.py
    - pickem/pickem_homepage/urls.py
    - pickem/pickem_homepage/templates/pickem/family_pool_home.html
    - pickem/pickem_homepage/tests.py
  key_links:
    - `index()` authenticated branch redirects before legacy global home queries execute.
    - `family_pool_home()` uses `request.tenant_context.pool` for competition data and `request.tenant_context.family` for community data.
    - Dashboard links point to tenant route names when current family/pool context exists.
---

<objective>
Migrate the dashboard/home entry point into explicit family/pool context and stop signed-in users from seeing global private dashboard data.

Purpose: Phase 4 starts by protecting the first signed-in app surface users reach.
Output: Tenant dashboard tests, tenant dashboard data scoping, and signed-in legacy dashboard redirects.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-family-scoped-app-pages/04-CONTEXT.md
@.planning/phases/04-family-scoped-app-pages/04-RESEARCH.md
@.planning/phases/04-family-scoped-app-pages/04-PATTERNS.md
@.planning/phases/03-onboarding-and-family-selection/03-01-SUMMARY.md
@.planning/phases/03-onboarding-and-family-selection/03-04-SUMMARY.md
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/urls.py
@pickem/pickem_homepage/templates/pickem/home.html
@pickem/pickem_homepage/templates/pickem/family_pool_home.html
@pickem/pickem_homepage/tests.py
@pickem/pickem_api/authz.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add dashboard tenant isolation tests</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <read_first>pickem/pickem_homepage/tests.py, .planning/phases/04-family-scoped-app-pages/04-PATTERNS.md, .planning/phases/03-onboarding-and-family-selection/03-01-SUMMARY.md</read_first>
  <action>Add focused Django client tests for D-01, D-02, D-03, D-04, D-09, D-10, D-12, D-22, D-23, and D-24. Cover anonymous `/` public rendering, signed-in `/` redirect by membership count, direct tenant dashboard access for a member, outsider denial, and dashboard response content that includes current-family data while excluding another family's standings rows, pick counts, and message-board posts. Use existing `_family_with_pool` and active-membership fixture patterns; create two families, two pools, same-season games, pool-scoped `userSeasonPoints`/`GamePicks`, and family-scoped `MessageBoardPost` rows.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tests demonstrate that another family's dashboard names, posts, picks, and standings do not render in the current tenant page.
    - Tests preserve anonymous public home behavior and Phase 3 onboarding/picker behavior.
  </acceptance_criteria>
  <done>Dashboard isolation behavior is expressed in automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Scope tenant dashboard queries</name>
  <files>pickem/pickem_homepage/views.py</files>
  <read_first>pickem/pickem_homepage/views.py, pickem/pickem_api/models.py, pickem/pickem_homepage/models.py, .planning/phases/04-family-scoped-app-pages/04-RESEARCH.md</read_first>
  <action>Replace the lightweight `family_pool_home()` body with a tenant dashboard view that starts from `request.tenant_context`, derives season/competition from `tenant_context.pool`, keeps `GamesAndScores`, `GameWeeks`, and `Teams` as global NFL facts per D-05, and scopes private overlays by `pool=tenant_context.pool` or `family=tenant_context.family` per D-09 and D-10. Include current pool standings, recent weekly winners, current-week pick status for `request.user`, family message preview, and active family member activity. Avoid cache or memoized dashboard data unless the key includes family/pool scope per D-12. Keep `index()` signed-in behavior as redirect-only before global dashboard queries per D-02 and D-03.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - `family_pool_home()` never queries `GamePicks`, `userSeasonPoints`, `userStats`, `MessageBoardPost`, `MessageBoardVote`, or member rows for tenant dashboard widgets without current pool/family scope.
    - Signed-in `/` does not render legacy global private dashboard widgets.
    - Current family/pool names and Phase 3 header context remain visible.
  </acceptance_criteria>
  <done>Tenant dashboard data is current-family/current-pool scoped and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Render tenant dashboard links and empty states</name>
  <files>pickem/pickem_homepage/templates/pickem/family_pool_home.html, pickem/pickem_homepage/templates/pickem/home.html, pickem/pickem_homepage/urls.py</files>
  <read_first>pickem/pickem_homepage/templates/pickem/family_pool_home.html, pickem/pickem_homepage/templates/pickem/home.html, pickem/pickem_homepage/urls.py, .planning/phases/04-family-scoped-app-pages/04-PATTERNS.md</read_first>
  <action>Update the tenant dashboard template to render scoped dashboard sections and tenant-preserving links for dashboard calls to action per D-21 and D-22. Keep anonymous `home.html` public-safe. Add only route names needed by this dashboard plan, and leave unbuilt tenant page links as disabled current-context empty states until their owning plans add the concrete route names. Do not add owner/admin editing controls or settings mutation forms per D-19.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tenant dashboard links preserve `family_slug` and `pool_slug` where the destination route exists.
    - Empty states stay inside the current tenant context and do not link to global gameplay pages.
    - Anonymous public home remains usable.
  </acceptance_criteria>
  <done>Tenant dashboard markup supports the scoped view without leaking global app links.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->tenant dashboard | User-controlled family/pool slugs enter Django route resolution. |
| tenant dashboard->database | View queries private standings, picks, members, and message-board rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `family_pool_home()` dashboard queries | high | mitigate | Use `family_member_required`; filter private rows by current `tenant_context.pool` or `tenant_context.family`; add cross-family negative tests. |
| T-04-02 | Tampering | legacy signed-in root route | medium | mitigate | Redirect signed-in users before legacy global private query execution. |
| T-04-SC | Tampering | package installs | high | mitigate | No package installs are planned; reject any install attempt unless a Package Legitimacy Audit is added first. |
</threat_model>

<verification>
- [ ] `cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2`
- [ ] `cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings`
</verification>

<success_criteria>
- Signed-in dashboard entry is tenant-routed and no longer renders private global data.
- Tenant dashboard private widgets are scoped to the current family/pool.
- Anonymous public home behavior is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/04-family-scoped-app-pages/04-01-SUMMARY.md`.
</output>
