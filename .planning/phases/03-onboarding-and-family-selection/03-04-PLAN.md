---
phase: 03-onboarding-and-family-selection
plan: 04
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - pickem/pickem/context_processors.py
  - pickem/pickem/settings.py
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/templates/pickem/base.html
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements:
  - TEN-01
  - AUTHZ-01
  - INV-04
user_setup: []
must_haves:
  truths:
    - Authenticated users with active memberships see current family/pool context in the header.
    - Users with multiple active memberships can switch between family default-pool tenant URLs.
    - Header switcher data includes only active memberships for the current user.
    - No-family users see onboarding actions instead of a switcher containing global or inaccessible data.
    - UI visibility does not replace server-side tenant guards.
  artifacts:
    - A family context provider or view helper.
    - Updated desktop and mobile header controls in `base.html`.
    - Tests covering switcher context isolation.
  key_links:
    - Header switcher uses `get_user_family_memberships()` or an equivalent Phase 2 helper.
    - Switcher links target `/families/<family_slug>/pools/<pool_slug>/`.
    - Existing tenant route guards remain the authorization boundary.
---

<objective>
Make the current family/pool context visible and switchable in the shared application header.

Purpose: Multi-family users need clear context and navigation without relying on hidden state.
Output: Header/mobile switcher data, template updates, and isolation tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-and-family-selection/03-CONTEXT.md
@.planning/phases/03-onboarding-and-family-selection/03-RESEARCH.md
@.planning/phases/03-onboarding-and-family-selection/03-PATTERNS.md
@.planning/phases/03-onboarding-and-family-selection/03-01-SUMMARY.md
@.planning/phases/03-onboarding-and-family-selection/03-03-SUMMARY.md
@pickem/pickem/context_processors.py
@pickem/pickem/settings.py
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/templates/pickem/base.html
@pickem/pickem_api/authz.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add switcher context isolation tests</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <read_first>pickem/pickem_homepage/tests.py, pickem/pickem_homepage/templates/pickem/base.html, .planning/phases/03-onboarding-and-family-selection/03-CONTEXT.md</read_first>
  <action>Add tests for decisions D-16 through D-24: a user with one family sees current family/pool context; a user with multiple families sees only their own active family options; inactive memberships and outsider families are absent; no-family users see onboarding/create/join actions instead of unrelated switcher entries. Use Django template response content assertions where practical.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tests create at least two families and prove cross-family names are absent for outsiders.
    - Tests cover both one-family and multi-family authenticated users.
  </acceptance_criteria>
  <done>Switcher behavior is specified with isolation-focused tests.</done>
</task>

<task type="auto">
  <name>Task 2: Provide current family switcher data</name>
  <files>pickem/pickem/context_processors.py, pickem/pickem/settings.py, pickem/pickem_homepage/views.py</files>
  <read_first>pickem/pickem/context_processors.py, pickem/pickem/settings.py, pickem/pickem_homepage/views.py, pickem/pickem_api/authz.py, .planning/phases/03-onboarding-and-family-selection/03-PATTERNS.md</read_first>
  <action>Add a context processor or shared helper that exposes current family, current pool, and active membership choices for authenticated requests. Resolve current context from explicit tenant route kwargs where available and from active memberships/default active pools otherwise. Do not include inactive memberships or families the user cannot access. Register any context processor in settings only if the existing project pattern supports it cleanly.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Switcher context is derived server-side from the authenticated user.
    - No client-supplied family id is trusted for membership data.
    - Existing pages without tenant route kwargs continue rendering.
  </acceptance_criteria>
  <done>Templates can render tenant context and switcher choices safely.</done>
</task>

<task type="auto">
  <name>Task 3: Update desktop and mobile header switcher</name>
  <files>pickem/pickem_homepage/templates/pickem/base.html</files>
  <read_first>pickem/pickem_homepage/templates/pickem/base.html, .planning/phases/03-onboarding-and-family-selection/03-PATTERNS.md</read_first>
  <action>Update the existing header/navigation markup to show the active family and default pool, plus a compact switcher for users with multiple active memberships. Keep mobile behavior consistent with existing base template patterns. Use readable tenant URLs for switch targets. Avoid broad visual redesign or Bootstrap/Tailwind migration work beyond this focused control.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Header shows current family/pool context on tenant pages.
    - Multi-family switch targets are valid tenant URLs.
    - No dead-end state appears for no-family users.
  </acceptance_criteria>
  <done>Header and mobile navigation expose tenant context without leaking other families.</done>
</task>
</tasks>

<verification>
- [ ] `cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2`
- [ ] `curl -s http://localhost:8000 | head -40` against the already-running local server, if available, to spot-check public home rendering.
</verification>

<success_criteria>
- Family context is visible in shared navigation.
- Multi-family switcher is server-side scoped to active memberships.
- Existing routes remain protected by server-side guards.
</success_criteria>

<output>
After completion, create `.planning/phases/03-onboarding-and-family-selection/03-04-SUMMARY.md`.
</output>
