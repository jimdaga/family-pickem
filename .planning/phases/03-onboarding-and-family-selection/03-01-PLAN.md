---
phase: 03-onboarding-and-family-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/urls.py
  - pickem/pickem_homepage/templates/pickem/onboarding.html
  - pickem/pickem_homepage/templates/pickem/family_picker.html
  - pickem/pickem_homepage/templates/pickem/family_pool_home.html
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements:
  - INV-03
  - INV-04
  - AUTHZ-01
user_setup: []
must_haves:
  truths:
    - Anonymous visitors can still render the public home page at `/`.
    - Authenticated users with zero active family memberships cannot see global private home data and are routed to onboarding first.
    - Authenticated users with one active membership are routed to that family's default active pool tenant URL.
    - Authenticated users with multiple active memberships are routed to a family picker that lists only their active memberships.
    - Direct tenant entry requires active family membership and pool-family consistency.
  artifacts:
    - pickem/pickem_homepage/templates/pickem/onboarding.html
    - pickem/pickem_homepage/templates/pickem/family_picker.html
    - pickem/pickem_homepage/templates/pickem/family_pool_home.html
  key_links:
    - `index()` authenticated branch calls `get_user_family_memberships()` before global standings, picks, posts, or stats are queried.
    - Tenant entry route `/families/<family_slug>/pools/<pool_slug>/` is protected by Phase 2 `family_member_required`.
    - Family picker links use each membership family's default active pool and readable tenant URLs.
---

<objective>
Route signed-in users into a valid family/pool context before they can see legacy global private data.

Purpose: Phase 3 begins the user-facing tenant journey without migrating gameplay pages yet.
Output: Root routing, no-family onboarding shell, multi-family picker, and lightweight family/pool entry page.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-and-family-selection/03-CONTEXT.md
@.planning/phases/03-onboarding-and-family-selection/03-RESEARCH.md
@.planning/phases/03-onboarding-and-family-selection/03-PATTERNS.md
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/urls.py
@pickem/pickem_homepage/templates/pickem/base.html
@pickem/pickem_homepage/templates/pickem/home.html
@pickem/pickem_homepage/tests.py
@pickem/pickem_api/authz.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add routing tests for membership-count entry</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <read_first>pickem/pickem_homepage/tests.py, pickem/pickem_api/tests.py, .planning/phases/03-onboarding-and-family-selection/03-CONTEXT.md</read_first>
  <action>Add focused Django client tests for decisions D-01 through D-04 and D-23 through D-26: anonymous GET `/` returns the public page; authenticated user with no active membership is redirected to or served onboarding; one active membership routes to `/families/<family_slug>/pools/<pool_slug>/`; multiple active memberships route to a picker listing only those families; outsider direct tenant entry is denied. Use existing test factory style from `pickem_homepage/tests.py` and Phase 2 family/pool authz fixtures rather than global commissioner state.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tests fail before implementation for missing onboarding, picker, and tenant entry behavior.
    - Tests create at least two families and prove an outsider cannot enter the other family tenant URL.
  </acceptance_criteria>
  <done>Focused routing tests express the Phase 3 membership-count contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement onboarding, picker, and tenant entry views</name>
  <files>pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py</files>
  <read_first>pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_api/authz.py, .planning/phases/03-onboarding-and-family-selection/03-PATTERNS.md</read_first>
  <action>Add helper code in `pickem_homepage/views.py` for resolving the default active pool for a family without legacy fallback. Add `onboarding`, `family_picker`, and `family_pool_home` views. Update `index()` so authenticated users branch by active memberships before any legacy global home queries run. Add URL names for onboarding, family picker, and `families/<slug:family_slug>/pools/<slug:pool_slug>/`. Follow the mapped view/form/auth patterns from `03-PATTERNS.md`, and protect tenant entry with `family_member_required` or equivalent Phase 2 guard.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - `index()` keeps anonymous public home behavior.
    - Authenticated no-family users reach onboarding before legacy standings, picks, posts, or stats are queried.
    - Tenant entry rejects outsiders and inactive memberships.
    - A family with no active pool renders a safe unavailable state instead of falling back to global data.
  </acceptance_criteria>
  <done>Membership-count routing and tenant entry views are implemented and covered.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused onboarding and picker templates</name>
  <files>pickem/pickem_homepage/templates/pickem/onboarding.html, pickem/pickem_homepage/templates/pickem/family_picker.html, pickem/pickem_homepage/templates/pickem/family_pool_home.html</files>
  <read_first>pickem/pickem_homepage/templates/pickem/base.html, pickem/pickem_homepage/templates/pickem/home.html, .planning/phases/03-onboarding-and-family-selection/03-PATTERNS.md</read_first>
  <action>Create templates that extend the existing base layout and match the current Tailwind-moving style. Onboarding must show create-family and join-by-code paths without showing dashboard, standings, picks, or player data. The picker must show only the current user's active family/default-pool choices. The family/pool home is a lightweight bridge page with clear family/pool context and compatibility links to existing pages for Phase 4 migration.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Templates render through the Django test client.
    - Onboarding and picker have empty-state copy and no global private data tables.
    - Tenant home visibly includes current family and pool names.
  </acceptance_criteria>
  <done>Templates support the routing shell and tests pass.</done>
</task>
</tasks>

<verification>
- [ ] `cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2`
- [ ] `cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings`
</verification>

<success_criteria>
- Signed-in users enter onboarding, picker, or tenant home according to active membership count.
- No-family authenticated users cannot see global private home data.
- Tenant entry authorization uses server-side membership checks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-onboarding-and-family-selection/03-01-SUMMARY.md`.
</output>
