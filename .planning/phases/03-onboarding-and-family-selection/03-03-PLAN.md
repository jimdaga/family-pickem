---
phase: 03-onboarding-and-family-selection
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - pickem/pickem_homepage/forms.py
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/urls.py
  - pickem/pickem_homepage/templates/pickem/join_family.html
  - pickem/pickem_homepage/templates/pickem/family_pool_home.html
  - pickem/pickem_homepage/tests.py
  - pickem/pickem_api/tests.py
autonomous: true
requirements:
  - INV-01
  - INV-03
  - AUTHZ-01
  - AUD-01
user_setup: []
must_haves:
  truths:
    - Family owners can create a member invite from family/pool context.
    - Non-owners cannot create Phase 3 invites.
    - Invite raw codes are shown only at creation/acceptance time and are not stored in the database.
    - Invite acceptance supports link and manual code entry for authenticated users.
    - Revoked, expired, exhausted, invalid, inactive-family, inactive-pool, and pool-family mismatch invites do not create memberships.
    - A valid invite can create or reactivate a member membership for the invitation family.
  artifacts:
    - Join-family form and route.
    - Owner-only invite creation handler.
    - pickem/pickem_homepage/templates/pickem/join_family.html
  key_links:
    - Invite creation writes `FamilyInvitation.code_hash`, not the raw code.
    - Invite acceptance hashes submitted codes server-side before lookup.
    - Successful acceptance redirects to the invited family/pool tenant URL.
    - Invite actions write `FamilyAuditLog` entries for creation and acceptance.
---

<objective>
Add the minimal secure invite path needed for a new family owner to invite members during onboarding.

Purpose: Families need a join path in Phase 3, while full invite management remains Phase 5.
Output: Owner-only invite creation, join-by-code/link acceptance, tests, and join template.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-and-family-selection/03-CONTEXT.md
@.planning/phases/03-onboarding-and-family-selection/03-RESEARCH.md
@.planning/phases/03-onboarding-and-family-selection/03-PATTERNS.md
@.planning/phases/03-onboarding-and-family-selection/03-01-SUMMARY.md
@.planning/phases/03-onboarding-and-family-selection/03-02-SUMMARY.md
@pickem/pickem_homepage/forms.py
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/urls.py
@pickem/pickem_api/models.py
@pickem/pickem_api/authz.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add invite creation and acceptance tests</name>
  <files>pickem/pickem_homepage/tests.py, pickem/pickem_api/tests.py</files>
  <read_first>pickem/pickem_homepage/tests.py, pickem/pickem_api/tests.py, pickem/pickem_api/models.py, .planning/phases/03-onboarding-and-family-selection/03-CONTEXT.md, .planning/phases/03-onboarding-and-family-selection/03-RESEARCH.md</read_first>
  <action>Add tests for decisions D-09 through D-15 and D-28: owner can create a member invite; admin/member/outsider cannot create one in Phase 3; created invitation stores only a hash; invite defaults are fourteen days and twenty uses; authenticated acceptance by link or manual code creates active member membership; an inactive same-family membership is reactivated; invalid, revoked, expired, exhausted, inactive-family, inactive-pool, and pool-family mismatch inputs fail with generic safe errors and no membership. Include CSRF-enforcing POST tests for creation and acceptance.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage pickem_api --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tests prove raw invite codes are not persisted.
    - Tests prove non-owners cannot create invites.
    - Tests prove failure cases do not reveal whether a family exists beyond a generic invalid-invite outcome.
  </acceptance_criteria>
  <done>Invite security behavior is specified by negative and positive tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement owner-only invite creation</name>
  <files>pickem/pickem_homepage/forms.py, pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_homepage/templates/pickem/family_pool_home.html</files>
  <read_first>pickem/pickem_homepage/views.py, pickem/pickem_homepage/forms.py, pickem/pickem_api/models.py, pickem/pickem_api/authz.py, .planning/phases/03-onboarding-and-family-selection/03-PATTERNS.md</read_first>
  <action>Add an owner-only POST handler in the family/pool context to create a `FamilyInvitation` with role `member`, active invited family/pool, `expires_at` fourteen days from creation, and `max_uses` twenty. Generate a high-entropy raw code with Python secrets, store only a server-side hash in `code_hash`, display the raw code/link once in the response or redirect message, and audit invite creation. Do not implement list/revoke/regenerate/admin invite management in Phase 3.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage pickem_api --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Creation requires active owner membership in the route family.
    - Database rows contain `code_hash` and do not contain the raw invite code.
    - The route is POST-only for mutation and CSRF-protected.
  </acceptance_criteria>
  <done>Family owners can produce one-time-visible member invite codes safely.</done>
</task>

<task type="auto">
  <name>Task 3: Implement join-by-code and link acceptance</name>
  <files>pickem/pickem_homepage/forms.py, pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_homepage/templates/pickem/join_family.html, pickem/pickem_homepage/templates/pickem/onboarding.html</files>
  <read_first>pickem/pickem_homepage/views.py, pickem/pickem_homepage/forms.py, pickem/pickem_api/models.py, .planning/phases/03-onboarding-and-family-selection/03-RESEARCH.md</read_first>
  <action>Add join-family route(s) for manual code entry and invite links. Require authentication before acceptance. Normalize submitted code for transport/readability, hash server-side, validate active/non-revoked/non-expired/not-exhausted invitation, validate active family and pool-family consistency, then create or reactivate an active member membership in a transaction. Increment invite use count and audit acceptance. Update onboarding to expose the join path.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage pickem_api --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Anonymous users are redirected through login before acceptance.
    - Successful acceptance lands on the invited tenant home.
    - Invalid acceptance paths do not create or reactivate memberships.
  </acceptance_criteria>
  <done>Users can join a family by invite code/link with tested isolation safeguards.</done>
</task>
</tasks>

<verification>
- [ ] `cd pickem && ../venv/bin/python manage.py test pickem_homepage pickem_api --settings=pickem.test_settings --verbosity=2`
- [ ] `cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings`
</verification>

<success_criteria>
- Minimal invite creation and acceptance work without storing raw codes.
- Owner-only authorization, CSRF, and invalid invite cases are tested.
- Full invite management remains deferred to Phase 5.
</success_criteria>

<output>
After completion, create `.planning/phases/03-onboarding-and-family-selection/03-03-SUMMARY.md`.
</output>
