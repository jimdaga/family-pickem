---
phase: 03-onboarding-and-family-selection
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - pickem/pickem_homepage/forms.py
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/urls.py
  - pickem/pickem_homepage/templates/pickem/create_family.html
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements:
  - TEN-01
  - AUTHZ-01
  - AUD-01
user_setup: []
must_haves:
  truths:
    - Authenticated users can create a family by submitting only a family name.
    - Creating a family creates exactly one active current-season NFL pool, pool settings, and an owner membership in one transaction.
    - Client input cannot choose owner user, role, status, pool season, or family id.
    - Slug collisions are handled deterministically with unique family and pool slugs.
    - Family creation writes security-relevant audit log rows.
  artifacts:
    - A create-family form in `pickem_homepage/forms.py`.
    - A create-family route and view in `pickem_homepage`.
    - pickem/pickem_homepage/templates/pickem/create_family.html
  key_links:
    - Onboarding create action links to the create-family route.
    - Successful create-family redirects to `/families/<family_slug>/pools/<pool_slug>/`.
    - Created `FamilyMembership.role` is `owner` for `request.user`.
---

<objective>
Add the create-family onboarding flow that creates the tenant and default pool context in one reviewed slice.

Purpose: A new signed-in user needs a self-service path into an isolated family/pool.
Output: Create-family form, view, template, tests, and audit-backed default pool creation.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-and-family-selection/03-CONTEXT.md
@.planning/phases/03-onboarding-and-family-selection/03-RESEARCH.md
@.planning/phases/03-onboarding-and-family-selection/03-PATTERNS.md
@.planning/phases/03-onboarding-and-family-selection/03-01-SUMMARY.md
@pickem/pickem_homepage/forms.py
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/urls.py
@pickem/pickem_api/models.py
@pickem/pickem/utils.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add create-family tests</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <read_first>pickem/pickem_homepage/tests.py, pickem/pickem_api/models.py, .planning/phases/03-onboarding-and-family-selection/03-CONTEXT.md</read_first>
  <action>Add tests for decisions D-05 through D-08: GET create-family requires login; POST with a valid family name creates `Family`, current-season default `Pool`, `PoolSettings`, owner `FamilyMembership`, and `FamilyAuditLog`; slug collisions receive unique slugs; POST ignores or rejects any client-supplied role, owner, status, season, family id, or pool id fields. Include CSRF coverage using Django's CSRF-enforcing test client for the POST path.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Tests prove the creator is the only owner created by the flow.
    - Tests prove the default pool belongs to the created family.
    - Tests prove CSRF is required on mutation.
  </acceptance_criteria>
  <done>Create-family behavior is specified by failing tests before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement transactional create-family flow</name>
  <files>pickem/pickem_homepage/forms.py, pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py</files>
  <read_first>pickem/pickem_homepage/forms.py, pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_api/models.py, pickem/pickem/utils.py, .planning/phases/03-onboarding-and-family-selection/03-PATTERNS.md</read_first>
  <action>Add a small family-name form and authenticated create-family view. On valid POST, use `transaction.atomic()` to create `Family`, default current-season NFL `Pool`, `PoolSettings`, active owner `FamilyMembership`, and `FamilyAuditLog` entries. Generate unique slugs server-side. The form must not expose tenant ids, owner ids, role, status, season, or pool fields. Redirect to the new tenant home URL on success. Follow existing Django form/view patterns mapped in `03-PATTERNS.md`.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Create-family route is POST-mutating and CSRF-protected by Django middleware.
    - Database creation is atomic.
    - Audit rows include family creation and owner membership creation or equivalent security-sensitive action records.
  </acceptance_criteria>
  <done>Create-family flow creates a complete default tenant context safely.</done>
</task>

<task type="auto">
  <name>Task 3: Wire create-family template from onboarding</name>
  <files>pickem/pickem_homepage/templates/pickem/create_family.html, pickem/pickem_homepage/templates/pickem/onboarding.html</files>
  <read_first>pickem/pickem_homepage/templates/pickem/base.html, pickem/pickem_homepage/templates/pickem/onboarding.html, .planning/phases/03-onboarding-and-family-selection/03-PATTERNS.md</read_first>
  <action>Add the create-family template and update onboarding to link to it. Keep the screen focused on family name only. Render form errors and loading/error states using existing template style; do not add pool configuration or family admin management UI in this plan.</action>
  <verify><automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated></verify>
  <acceptance_criteria>
    - Onboarding exposes create-family without requiring dashboard access.
    - Create-family form renders validation errors.
    - Successful flow lands on tenant home.
  </acceptance_criteria>
  <done>Users can navigate from onboarding to family creation and into tenant context.</done>
</task>
</tasks>

<verification>
- [ ] `cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2`
- [ ] `cd pickem && ../venv/bin/python manage.py makemigrations --check --dry-run --settings=pickem.test_settings`
</verification>

<success_criteria>
- Create-family is self-service, transactional, and server-owned.
- Default pool context exists immediately after creation.
- Mutation is CSRF-protected and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/03-onboarding-and-family-selection/03-02-SUMMARY.md`.
</output>
