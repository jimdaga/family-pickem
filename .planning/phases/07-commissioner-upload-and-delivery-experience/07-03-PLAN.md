---
phase: 07-commissioner-upload-and-delivery-experience
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - pickem/pickem_homepage/templates/pickem/family_picker.html
  - pickem/pickem_homepage/templates/pickem/family_pool_home.html
  - pickem/pickem_homepage/tests.py
  - .planning/phases/07-commissioner-upload-and-delivery-experience/07-VALIDATION.md
autonomous: true
requirements: [LOGO-03, LOGO-04, S3-04]
must_haves:
  truths:
    - "Settings, family picker, and family home render only a server-controlled canonical logo URL or a single static default; no original upload reference, legacy URL, or browser object URL is rendered."
    - "Picker and home use compact neutral rounded-square marks beside visible family names with decorative empty alt text; settings retains its 96px editor preview."
    - "A saved removal renders the same default mark everywhere after redirect, and staged browser removal cannot affect other pages before Save."
    - "Focused tests and final checks prove safe delivery/rendering while Phase 8 IAM/ESO, hostile authorization matrix, and obsolete object lifecycle remain excluded."
  artifacts:
    - path: pickem/pickem_homepage/templates/pickem/family_picker.html
      provides: "Unified compact controlled family mark for member and superadmin choices"
    - path: pickem/pickem_homepage/templates/pickem/family_pool_home.html
      provides: "Controlled lobby family mark with decorative semantics"
    - path: pickem/pickem_homepage/tests.py
      provides: "Cross-surface canonical/default/accessibility regression tests"
  key_links:
    - "Family.logo generated private storage URL -> templates; absent field -> identical Django static fallback"
    - "successful staged removal -> redirect -> Family.logo null -> settings/picker/lobby fallback branch"
---

<objective>
Finish the visible family-brand delivery contract and prove the Phase 7 experience renders only canonical processed assets or the default fallback across every agreed surface.

Purpose: complete LOGO-04 and S3-04 without expanding into Phase 8 cloud/IAM/lifecycle scope.
Output: unified picker/lobby rendering and end-to-end Phase 7 regression/validation evidence.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-CONTEXT.md
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-RESEARCH.md
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-PATTERNS.md
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-VALIDATION.md
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-01-SUMMARY.md
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-02-SUMMARY.md
@pickem/pickem_homepage/templates/pickem/family_picker.html
@pickem/pickem_homepage/templates/pickem/family_pool_home.html
@pickem/pickem_homepage/tests.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify compact controlled logo rendering on picker and family home</name>
  <files>pickem/pickem_homepage/templates/pickem/family_picker.html, pickem/pickem_homepage/templates/pickem/family_pool_home.html, pickem/pickem_homepage/tests.py</files>
  <behavior>
    - Test picker member and superadmin choice variants use the same canonical-or-static source branch and compact rounded-square wrapper.
    - Test family home renders the same controlled/default pattern beside its visible family name.
    - Test every adjacent-name mark is decorative with empty alt and no family-name repetition.
  </behavior>
  <read_first>Read both template variants fully, the current logo static assets, settings editor fallback choice from Plan 02, and existing family picker/home response tests. Confirm private storage `.url` behavior is already controlled by Phase 6 and do not add a public endpoint.</read_first>
  <action>Apply D-12 through D-15 consistently. In both family-picker button variants and family home/dashboard, render `Family.logo.url` only when the server-owned canonical field is present; otherwise render the same approved Django static Family Pick'em mark used by settings. Wrap each in a compact neutral `h-8 w-8` rounded-square `overflow-hidden` frame with `object-contain` so transparent and opaque marks share the same intentional presentation. Because visible sibling text names the family, use `alt=""` and `aria-hidden="true"`; preserve meaningful text labels and link targets. Do not render old `logo_url`, an original filename, arbitrary image URL, object URL, client crop output, or storage internals. Extend existing Django route tests for saved logo, null/default fallback, both picker variants, family home, exact accessibility semantics, and absence of legacy fields.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests --settings=pickem.test_settings --verbosity=1</automated>
    <automated>if rg -n -e 'logo_url' -e 'rounded-full' -e 'alt="{{ choice.family.name }} logo"' -e 'alt="{{ family.name }} logo"' pickem/pickem_homepage/templates/pickem/family_picker.html pickem/pickem_homepage/templates/pickem/family_pool_home.html; then exit 1; else status=$?; test "$status" -eq 1; fi</automated>
  </verify>
  <done>All agreed family surfaces use one compact accessible canonical/default mark contract; no arbitrary user-controlled image reference reaches markup.</done>
</task>

<task type="auto">
  <name>Task 2: Run Phase 7 integration regression and record final validation evidence</name>
  <files>pickem/pickem_homepage/tests.py, .planning/phases/07-commissioner-upload-and-delivery-experience/07-VALIDATION.md</files>
  <read_first>Read all Plan 01/02 implementation summaries, `07-VALIDATION.md`, Phase 6 verification, and the route/template tests modified in this phase. Reconcile the validation task table with executed coverage; do not claim browser checks that were not actually performed.</read_first>
  <action>Complete focused integration tests that chain a valid upload/crop or a valid staged removal through the existing authorized settings POST and then GET settings, picker, and lobby: after redirect, assert only the canonical generated `.webp` URL or static default appears and no local preview/blob/original source is reflected. Preserve regression coverage for invalid crop/image error behavior, old-logo preservation, no-JS omitted-crop centered behavior, CSRF/tenant enforcement inherited from Phase 6, and renderer accessibility. Run all required Django, migration, and static-build checks; update the Phase validation task statuses and manual UAT record accurately. Use the existing local server and curl to inspect rendered settings/picker/lobby HTML after implementation as AGENTS.md requires. Do not add AWS credentials, IAM/ESO changes, signed URL policy work, full hostile cross-family matrix, or old-object deletion lifecycle; those are Phase 8 fences.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test --settings=pickem.test_settings --verbosity=1</automated>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py check --settings=pickem.test_settings &amp;&amp; ../venv/bin/python manage.py makemigrations --check --dry-run --settings=pickem.test_settings</automated>
    <automated>npm run build:css:once &amp;&amp; npm run build:logo-editor-assets &amp;&amp; git diff --check</automated>
    <human_check>Using the existing local server, inspect the authenticated settings page and its picker/lobby follow-up with curl/browser after a save and after removal; exercise the Phase 7 manual crop/EXIF/mobile steps documented in 07-VALIDATION.md.</human_check>
  </verify>
  <done>Phase 7 evidence shows end-to-end controlled rendering, accessible fallback, and retained Phase 6 safeguards, with manual editor verification explicitly recorded rather than inferred.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Risk | Mitigation |
|---|---|---|---|
| T-07-09 | XSS/information disclosure | Templates render original/local/arbitrary image references. | Branch solely on server-controlled `Family.logo` and `{% static %}` default; tests reject legacy/object-url paths. |
| T-07-10 | Accessibility/UX integrity | Opaque/transparent assets or fallback vary across family surfaces. | Shared neutral rounded-square/object-contain contract and decorative semantics beside visible names. |
| T-07-11 | Tampering | Staged removal leaks before server commit. | Other surfaces read persisted DB state only after redirect; browser state is local to settings. |
</threat_model>

<artifacts_this_phase_produces>
- Consistent compact picker and family-home family marks.
- Cross-surface controlled asset/default regression tests.
- Completed Phase 7 validation record with automated and manual evidence.
</artifacts_this_phase_produces>

<verification>
Run focused then full Django tests, checks, migration dry-run, static build, and diff check. Validate the browser/editor manually because this repository has no established frontend DOM test harness. Confirm no Phase 8 infrastructure/lifecycle changes enter the diff.
</verification>

<success_criteria>
After a successful save or removal, every agreed family surface renders the canonical processed logo or the accessible default mark, with no original upload or arbitrary image path exposed.
</success_criteria>

<output>
Create `.planning/phases/07-commissioner-upload-and-delivery-experience/07-03-SUMMARY.md` when done.
</output>
