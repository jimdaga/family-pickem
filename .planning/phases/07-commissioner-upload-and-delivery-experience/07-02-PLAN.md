---
phase: 07-commissioner-upload-and-delivery-experience
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - pickem/pickem_homepage/templates/pickem/family_admin_settings.html
  - pickem/pickem_homepage/static/js/family-logo-editor.js
  - pickem/pickem_homepage/static/vendor/cropperjs/cropper.js
  - pickem/pickem_homepage/static/vendor/cropperjs/cropper.css
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements: [LOGO-01, LOGO-02, LOGO-03]
must_haves:
  truths:
    - "The settings page retains an accessible native multipart chooser and works with JavaScript disabled using the server centered crop."
    - "With JavaScript, selecting a file creates only a local object-URL preview in a large rounded-square fixed-square Cropper editor with drag, zoom slider, and centered Reset."
    - "The page submits the original File plus strict source-pixel crop fields, never a canvas export, browser blob URL, separate upload, or direct S3 request."
    - "Replacement, Clear selection, and confirmed staged removal are mutually exclusive local states until the existing Save settings submit succeeds."
  artifacts:
    - path: pickem/pickem_homepage/templates/pickem/family_admin_settings.html
      provides: "Progressively enhanced accessible logo editor markup and staged actions"
    - path: pickem/pickem_homepage/static/js/family-logo-editor.js
      provides: "Guarded local-only Cropper.js state controller"
  key_links:
    - "file input -> local URL.createObjectURL -> orientation-safe Cropper preview -> hidden integer crop fields -> normal multipart submit"
    - "Reset -> centered crop while retaining File; Clear selection -> server controlled current/default image; confirmed removal -> hidden remove_logo + default preview"
---

<objective>
Build the commissioner-facing progressive crop/editor flow exactly as locked in D-01 through D-11, using only the Phase 6 secure multipart submit boundary.

Purpose: give admins an understandable preview/reposition/zoom workflow while keeping local preview state entirely non-authoritative.
Output: responsive settings markup and a lifecycle-safe local editor controller.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-CONTEXT.md
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-RESEARCH.md
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-PATTERNS.md
@.planning/phases/06-secure-logo-foundation/06-UI-SPEC.md
@.planning/phases/06-secure-logo-foundation/06-VERIFICATION.md
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-01-SUMMARY.md
@pickem/pickem_homepage/templates/pickem/family_admin_settings.html
@pickem/pickem_homepage/static/js/family-logo-editor.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the accessible no-JS editor baseline and test its server contract</name>
  <files>pickem/pickem_homepage/templates/pickem/family_admin_settings.html, pickem/pickem_homepage/tests.py</files>
  <behavior>
    - Test the settings form remains multipart and has the native chooser, four hidden crop inputs, hidden removal input, and editor/action hooks.
    - Test current/default image branches only use the canonical Family.logo URL or one chosen static default asset.
    - Test server-rendered errors retain ordinary bound values and the old current/default mark after rejected submission.
  </behavior>
  <read_first>Read the full Phase 6 UI-SPEC and the existing settings template's Tailwind/error/message conventions. Inspect the actual static default assets and select one consistently for all Phase 7 surfaces; keep the no-JS semantic markup functional before loading any editor script.</read_first>
  <action>Replace the Phase 6 thumbnail section with the D-01/D-02/D-05 96px settings editor baseline: a neutral light/dark-aware card surface and rounded-square `overflow-hidden` frame, with a current canonical logo or static default fallback. Keep the native visible chooser label/help/error association and multipart form; render hidden crop fields and `remove_logo` for enhancement while absent crop values preserve the server centered crop. Add stable IDs/data hooks for chooser, server preview, editor image/container, zoom slider, Reset, Clear selection, Remove logo, each crop value, and removal state. Render Remove logo only where a saved server logo exists (D-09); render Clear selection only as an enhancement state for a local file (D-11). Ensure error markup is an associated alert and clearly says the file must be selected again after a failed request (D-08). Do not hide or replace the native chooser, create a separate Apply action, add drag-and-drop, or claim that a local preview is saved.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests --settings=pickem.test_settings --verbosity=1</automated>
    <automated>if rg -n -e 'logo_url' -e 'https?://.*cropper' -e 'rounded-full' pickem/pickem_homepage/templates/pickem/family_admin_settings.html; then exit 1; else status=$?; test "$status" -eq 1; fi</automated>
  </verify>
  <done>The settings form is usable without JavaScript and exposes the exact accessible enhancement hooks while rendering only controlled current/default imagery.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the local-only Cropper.js controller with orientation-safe fallback</name>
  <files>pickem/pickem_homepage/static/js/family-logo-editor.js, pickem/pickem_homepage/templates/pickem/family_admin_settings.html, pickem/pickem_homepage/tests.py</files>
  <read_first>Read the actual local Cropper.js 2.1.1 distribution/API installed by Plan 01, especially selection and image reset/transform APIs. Read `process_family_logo` to preserve its EXIF-transposed coordinate contract. Do not import v1 usage examples or invent a canvas submission API.</read_first>
  <action>Load only the local vetted Cropper CSS/JS and implement a guarded page-specific controller that exits harmlessly if hooks or Cropper are unavailable. On a valid native selection, create one object URL, initialize a single fixed 1:1 selection with dimmed outside shade, allow image drag/reposition plus a simple zoom slider, and show the large rounded-square neutral editor preview (D-01, D-02, D-05). Maintain mutually exclusive `saved`, `selected-file`, and `remove-staged` state (D-03, D-06, D-10, D-11). Before normal form submit, read Cropper selection source coordinates, serialize exactly four safe decimal integer pixels with equal width/height; never serialize an object URL, data URL, canvas output, or image bytes. Reset must use Cropper.js 2.1.1 component APIs—`CropperImage.$resetTransform()` and `CropperSelection.$reset()` (or the exact equivalent confirmed in the installed distribution)—then restore the centered fixed-square selection while retaining the selected file (D-04). Do not call the v1 `destroy` API: dispose a v2 editor by clearing its image source and removing the generated Cropper v2 canvas/component subtree from the dedicated editor host, nulling stored component references, and only then revoking the object URL. Apply that disposal sequence before replacement, Clear selection, staged removal, and `pagehide`; verify in browser devtools that the prior Cropper canvas is detached and its object URL cannot be loaded after each transition. Clear restores the saved/default server renderer without a mutation. On selection, cancel staged removal; on confirmed native `window.confirm` with the exact D-09 sentence, clear local selection/crop fields, set `remove_logo`, and display the static default until Save. A decline leaves state unchanged. For EXIF-oriented files, create an orientation-normalized preview only when the browser path can demonstrate coordinate alignment with the server's `ImageOps.exif_transpose`; include an EXIF-rotated fixture/manual verification. If alignment cannot be demonstrated in that browser, clear all crop fields before submit so the server deliberately uses the safe centered crop. No action may POST, delete, call S3, or upload separately from Save settings.</action>
  <verify>
    <automated>npm run build:logo-editor-assets &amp;&amp; test -s pickem/pickem_homepage/static/vendor/cropperjs/cropper.js &amp;&amp; test -s pickem/pickem_homepage/static/vendor/cropperjs/cropper.css</automated>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests --settings=pickem.test_settings --verbosity=1</automated>
    <human_check>With the existing local server, sign in as a family admin at desktop and mobile widths. Select transparent, opaque, and EXIF-rotated fixtures; verify rounded-square neutral preview, drag, slider, Reset, clear/reselection, exact staged-remove confirmation, no network request before Save, centered-safe fallback if EXIF coordinate alignment is unavailable, and after replace/clear/removal/pagehide verify the old v2 component subtree is gone and its object URL has been revoked.</human_check>
  </verify>
  <done>The page offers the requested local Cropper interaction while the only network mutation remains the existing authorized multipart Save settings POST containing original file plus optional strict coordinates.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Risk | Mitigation |
|---|---|---|---|
| T-07-05 | Tampering | Browser cropper substitutes bytes, URL, or direct storage request. | Controller submits only original native File and four integers through regular form submit; server canonicalizer stays authoritative. |
| T-07-06 | Information disclosure | Object URLs survive state changes or appear in server markup. | Object URLs are never serialized and are revoked/destroyed on every lifecycle boundary and pagehide. |
| T-07-07 | Tampering | EXIF visual coordinates differ from server crop coordinates. | Orientation-safe preview is demonstrated with fixture; otherwise hidden crop values are cleared for server centered crop. |
| T-07-08 | Repudiation | UI removal deletes immediately or conflicts with a selected replacement. | Mutually exclusive local states, native confirmation, and sole Save settings commit point. |
</threat_model>

<artifacts_this_phase_produces>
- Accessible progressive editor markup and stable test hooks.
- Local lifecycle-safe Cropper controller.
- Browser UAT instructions including EXIF fallback.
</artifacts_this_phase_produces>

<verification>
Use focused Django tests for template/error contracts and browser UAT for drag/zoom/object-URL lifecycle. Inspect rendered HTML via the existing local server/curl. Do not add a JS unit-test framework solely for this phase; the validation strategy explicitly assigns crop interaction to browser UAT.
</verification>

<success_criteria>
An admin can preview, reposition, zoom, reset, replace, clear, or stage removal locally, while the no-JS path and Phase 6 server protections remain valid.
</success_criteria>

<output>
Create `.planning/phases/07-commissioner-upload-and-delivery-experience/07-02-SUMMARY.md` when done.
</output>
