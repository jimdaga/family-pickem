---
phase: 07-commissioner-upload-and-delivery-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - pickem/pickem_homepage/forms.py
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements: [LOGO-01, LOGO-02, LOGO-03, S3-04]
must_haves:
  truths:
    - "A normal multipart Save settings POST accepts an optional original source file plus either four strict square crop integers or no crop values, and passes only that mapping to the Phase 6 canonical processor."
    - "A valid staged removal clears only the locked Family.logo database reference on Save settings; a forged simultaneous replacement/removal submission has an explicit safe form error and preserves the old reference."
    - "The Cropper.js package is a locked local build dependency, never a CDN script, and browser crop output never becomes upload bytes."
    - "Failures preserve bound non-file settings and the prior controlled logo while requiring the browser to select its file again."
  artifacts:
    - path: package.json
      provides: "Pinned local Cropper.js dependency and deterministic asset-build entrypoint"
    - path: pickem/pickem_homepage/forms.py
      provides: "Strict crop/remove form contract"
      exports: [FamilyAdminSettingsForm]
    - path: pickem/pickem_homepage/views.py
      provides: "Tenant-locked crop forwarding and staged removal persistence"
      exports: [family_pool_admin_settings]
    - path: pickem/pickem_homepage/tests.py
      provides: "Form and settings-route regressions for crop/removal state"
  key_links:
    - "hidden crop fields -> FamilyAdminSettingsForm.clean -> crop_data -> process_family_logo(original file, crop_data)"
    - "remove_logo form boolean -> locked Family row -> reference clear only after valid Save settings POST"
    - "npm lockfile + explicit local asset build -> settings template; no network-loaded editor code"
---

<objective>
Prepare the server form contract and local vetted Cropper.js dependency needed by the progressive editor, without altering Phase 6's canonicalization, authorization, CSRF, streaming limit, storage, or compensating-persistence boundaries.

Purpose: make browser-supplied crop coordinates and staged removal untrusted but well-defined inputs to the existing settings submit path.
Output: locked package integration plus form/view/test seam for crop and removal.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-CONTEXT.md
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-RESEARCH.md
@.planning/phases/07-commissioner-upload-and-delivery-experience/07-PATTERNS.md
@.planning/phases/06-secure-logo-foundation/06-01-SUMMARY.md
@.planning/phases/06-secure-logo-foundation/06-03-SUMMARY.md
@pickem/pickem_api/logo_processing.py
@pickem/pickem_homepage/forms.py
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/tests.py
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define strict staged crop and removal inputs on the existing family settings form</name>
  <files>pickem/pickem_homepage/forms.py, pickem/pickem_homepage/tests.py</files>
  <behavior>
    - Test valid all-integer square crop fields become exactly `{x, y, width, height}` for the existing processor.
    - Test omitted crop fields produce `crop_data=None`, while partial, decimal, negative, non-square, or malformed values add a logo-field/form error before mutation.
    - Test a submission containing both a logo and removal request is rejected deliberately, leaving the old reference unchanged.
  </behavior>
  <read_first>Read `FamilyAdminSettingsForm`, `process_family_logo` and `validate_square_crop`, plus the Phase 6 logo test fixture/storage swap. Confirm the existing Django version's `IntegerField` and `BooleanField` cleaned-value behavior before choosing messages.</read_first>
  <action>Extend `FamilyAdminSettingsForm` with hidden optional crop inputs named `crop_x`, `crop_y`, `crop_width`, and `crop_height`, plus a hidden `remove_logo` boolean. In `clean()`, implement D-03/D-04's contract: all four crop fields are absent or all four are strict base-10 non-negative integer values; do not clamp, round, coerce floats, or trust browser dimensions. Build one `crop_data` mapping only when all values are present, and reject a partial/malformed/non-square set with a clear field-associated logo error. Keep `process_family_logo` and its post-EXIF bounds validation as final authority. Enforce an explicit mutually exclusive server contract: `remove_logo` combined with an uploaded `logo` is invalid rather than picking a winner from a tampered request. Add focused tests using the existing temporary local storage seam; invalid inputs must leave prior logo data untouched and preserve normal bound fields.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests --settings=pickem.test_settings --verbosity=1</automated>
  </verify>
  <done>Form-cleaned crop input is absent-or-complete and safe for the Phase 6 processor, and forged replacement/removal data cannot silently mutate a logo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist crop-based replacement and staged removal through the existing locked mutation</name>
  <files>pickem/pickem_homepage/views.py, pickem/pickem_homepage/tests.py</files>
  <behavior>
    - Test valid crop integers are supplied to the canonical processor and result in its generated WebP, not client canvas bytes.
    - Test a valid remove-only Save clears the DB reference and writes boolean-only audit metadata before redirecting with the established success message.
    - Test processor/form errors render the bound settings page with the old controlled logo and no redirect, audit, or storage mutation.
  </behavior>
  <read_first>Read the complete current `family_pool_admin_settings` transaction/row-lock/compensation flow and Phase 6 verification. Locate the exact `FamilyAuditLog` metadata convention and do not alter pre-CSRF multipart handling or storage cleanup behavior.</read_first>
  <action>Keep the existing decorated route, tenant-derived family/pool, CSRF path, pre-CSRF upload handler, `transaction.atomic`, locked Family row, canonical processor, generated-key storage, database persistence, audit logging, redirect, and storage compensation intact. For an uploaded file, pass only `form.cleaned_data['crop_data']` into `process_family_logo`; never accept browser-generated blob/canvas bytes, a client object key/URL, or an independently uploaded asset. For valid `remove_logo` with no upload, clear `locked_family.logo` inside the same locked valid settings transaction, persist only after Save settings, record before/after presence as boolean audit metadata, and use the existing redirect/message (D-07, D-09, D-10). Do not call `FieldFile.delete()` or broaden old-object lifecycle work. Map form/processor failures to the existing bound render so D-08 keeps ordinary values and the old server-controlled image but cannot repopulate the browser file input. Add integration regressions for coordinates, centered omitted crop, invalid crop preservation, remove success/audit, replacement+remove tampering, and owner/admin scope.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests --settings=pickem.test_settings --verbosity=1</automated>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_api.tests.test_logo_processing pickem_api.tests.test_family_logo_storage --settings=pickem.test_settings --verbosity=1</automated>
  </verify>
  <done>Replacement and removal remain one authorized Save settings mutation; only the Phase 6 canonical WebP is stored and all invalid/tampered input preserves the previous reference.</done>
</task>

<task type="auto">
  <name>Task 3: Install the vetted local Cropper.js package and deterministic static-asset build seam</name>
  <files>package.json, package-lock.json</files>
  <read_first>Read root `package.json`, its existing Tailwind scripts, and inspect `node_modules/cropperjs` only after installing the locked package. Use the researched official `cropperjs@2.1.1` package; verify its distributed JS/CSS names and license metadata instead of assuming a v1 API or adding a CDN URL.</read_first>
  <action>Add the researched legitimate `cropperjs` package at version `2.1.1` through npm so `package-lock.json` is synchronized. Add an explicit reproducible npm build/copy script compatible with this repository's no-JavaScript-bundler setup that places only the official Cropper JS and CSS distribution files into a declared local static vendor location for the settings template. Add a distinct finite `build:css:once` Tailwind script with the existing input/output paths but without the perpetual watch flag, retaining the existing watch script for local development and `build:prod` for minified release output. Do not introduce a remote script/style URL, a second frontend framework, a postinstall hook, or a browser-to-S3 path. Document the exact package artifacts in the script naming so the next plan can load them through Django staticfiles.</action>
  <verify>
    <automated>npm install --package-lock-only --ignore-scripts &amp;&amp; npm run build:css:once</automated>
    <automated>npm run build:logo-editor-assets &amp;&amp; test -s pickem/pickem_homepage/static/vendor/cropperjs/cropper.js &amp;&amp; test -s pickem/pickem_homepage/static/vendor/cropperjs/cropper.css</automated>
  </verify>
  <done>Cropper.js 2.1.1 is lockfile-pinned and served only as explicit repository-managed static assets, ready for progressive enhancement.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Risk | Mitigation |
|---|---|---|---|
| T-07-01 | Tampering | Crop fields attempt to escape source bounds or change server output behavior. | Form requires an all-or-none strict mapping; Phase 6 validates the mapping after EXIF normalization and emits the sole canonical WebP. |
| T-07-02 | Tampering | A crafted POST requests replacement and removal at once. | Explicit mutual-exclusion validation returns an error before any locked mutation. |
| T-07-03 | Elevation/repudiation | Remove attempt bypasses tenant/CSRF/audit controls. | Reuse the existing decorated locked settings transaction and boolean-only audit path. |
| T-07-04 | Supply chain/XSS | Browser loads mutable third-party crop code. | Pin audited 2.1.1 in npm lockfile and copy official assets locally; no CDN. |
</threat_model>

<artifacts_this_phase_produces>
- Strict crop/remove `FamilyAdminSettingsForm` contract.
- Existing settings route wired to safely forward crop input and stage removal on Save.
- Local locked Cropper.js assets build seam.
- Focused crop/removal regression coverage.
</artifacts_this_phase_produces>

<verification>
Run the focused homepage and logo-processing suites after each task. Confirm the installed static package is local, pinned, and has no CDN reference. This plan intentionally does not alter IAM, ESO, S3 policy, direct browser S3, or obsolete-object cleanup.
</verification>

<success_criteria>
The existing secure mutation can accept optional strict crop coordinates or staged removal without trusting browser image output, and Cropper.js is available locally for the next progressive UI plan.
</success_criteria>

<output>
Create `.planning/phases/07-commissioner-upload-and-delivery-experience/07-01-SUMMARY.md` when done.
</output>
