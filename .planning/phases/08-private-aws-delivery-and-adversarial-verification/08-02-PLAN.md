---
phase: 08-private-aws-delivery-and-adversarial-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - charts/family-pickem/templates/external-secret-logo-storage.yaml
  - charts/family-pickem/templates/deployment.yaml
  - charts/family-pickem/values.yaml
  - infra/app/values-dev.yaml
  - infra/app/values-prd.yaml
  - infra/family-logo-storage/iam-policy.json
  - infra/family-logo-storage/provision.sh
  - docs/runbooks/family-logo-storage.md
autonomous: true
requirements: [S3-02, S3-03]
user_setup:
  - service: AWS and Kubernetes
    why: "Execution uses authenticated AWS CLI and cluster contexts to create and sync managed secrets; no credential value is supplied in Git."
must_haves:
  truths:
    - "Each environment's dedicated principal can only GetObject, PutObject, and DeleteObject under family-pickem/family-logos/*."
    - "Dedicated logo credentials/configuration are held in family-pickem/{env}/family-logo-storage and reach Kubernetes only through ESO."
    - "A separate ExternalSecret is mounted only by migration and web containers, never backup jobs."
    - "Provisioning preflights the private bucket and simulates both intended IAM allows and privilege denials without printing secrets."
  artifacts:
    - path: infra/family-logo-storage/iam-policy.json
      provides: "Prefix-limited S3 IAM policy"
    - path: infra/family-logo-storage/provision.sh
      provides: "Idempotent AWS CLI provision/verify flow"
    - path: charts/family-pickem/templates/external-secret-logo-storage.yaml
      provides: "Dedicated ESO secret target"
    - path: docs/runbooks/family-logo-storage.md
      provides: "Redacted operations and release evidence procedure"
  key_links:
    - "Secrets Manager -> aws-secrets-manager ClusterSecretStore -> logo ExternalSecret -> migrate/app envFrom"
    - "IAM prefix policy -> FamilyLogoStorage generated family-logos/ keys"
---

<objective>
Implement D-02 and D-03: provision a least-privilege logo identity and deliver it through the existing AWS Secrets Manager, ESO, Helm, and Kubernetes path.

Purpose: keep the existing bucket private and prevent logo credentials reaching generic/backup workloads.
Output: IAM/provisioning sources, dedicated Helm secret wiring, environment values, and runbook.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-CONTEXT.md
@.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-RESEARCH.md
@.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-PATTERNS.md
@charts/family-pickem/templates/external-secret-envvars.yaml
@charts/family-pickem/templates/deployment.yaml
@charts/family-pickem/templates/backup-cronjob.yaml
@charts/family-pickem/values.yaml
@infra/app/values-dev.yaml
@infra/app/values-prd.yaml
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Render dedicated ESO logo-secret injection for only Django web and migration workloads</name>
  <files>charts/family-pickem/templates/external-secret-logo-storage.yaml, charts/family-pickem/templates/deployment.yaml, charts/family-pickem/values.yaml, infra/app/values-dev.yaml, infra/app/values-prd.yaml</files>
  <behavior>
    - Dev/prd renders contain a second owner-managed ExternalSecret using aws-secrets-manager and each environment's non-empty logo remote key.
    - Migration and web containers reference that target in addition to existing envvars only when the same logo-secret condition renders the ExternalSecret.
    - Backup CronJob has no logo-secret reference; default chart renders neither a logo ExternalSecret nor a logo-secret `envFrom` reference when no key is configured.
  </behavior>
  <action>Per D-03, add an empty-safe `externalSecrets.logoStorageKey` default and dev/prd values `family-pickem/dev/family-logo-storage` and `family-pickem/prd/family-logo-storage`. Define one shared Helm condition requiring both `externalSecrets.enabled` and a non-empty logo key; apply that same condition to the second v1 ExternalSecret and every logo-secret `envFrom` entry. The target must be a distinct owner-managed `<release>-logo-storage` Secret using the existing ClusterSecretStore. Under that condition inject it only into the migration init container and web container; outside it render neither the ExternalSecret nor any logo-secret reference. Preserve generic envvars, do not add plaintext values, and do not modify/inject backup CronJob. Add manifest-level rendered-chart assertions: default output has neither the logo ExternalSecret nor logo `envFrom`; dev/prd each contain exactly their remote key and exactly migration/web references; every rendered CronJob, including backup, has none.</action>
  <verify><automated>helm template family-pickem charts/family-pickem &gt; /tmp/fp-default.yaml &amp;&amp; helm template family-pickem-dev charts/family-pickem -f infra/app/values-dev.yaml &gt; /tmp/fp-dev.yaml &amp;&amp; helm template family-pickem-prd charts/family-pickem -f infra/app/values-prd.yaml &gt; /tmp/fp-prd.yaml &amp;&amp; ! rg -F -- '-logo-storage' /tmp/fp-default.yaml &amp;&amp; rg -F 'family-pickem/dev/family-logo-storage' /tmp/fp-dev.yaml &amp;&amp; rg -F 'family-pickem/prd/family-logo-storage' /tmp/fp-prd.yaml &amp;&amp; test "$(rg -c -- 'name: family-pickem-dev-logo-storage' /tmp/fp-dev.yaml)" -eq 3 &amp;&amp; test "$(rg -c -- 'name: family-pickem-prd-logo-storage' /tmp/fp-prd.yaml)" -eq 3 &amp;&amp; ! rg -n -A80 'name: .*backup' /tmp/fp-dev.yaml /tmp/fp-prd.yaml | rg -F -- '-logo-storage'</automated></verify>
  <done>Only required Django pods receive a separately reconciled secret, without chart-contained credentials or backup-job exposure.</done>
</task>

<task type="auto">
  <name>Task 2: Add idempotent AWS CLI provisioning and cloud verification runbook</name>
  <files>infra/family-logo-storage/iam-policy.json, infra/family-logo-storage/provision.sh, docs/runbooks/family-logo-storage.md</files>
  <action>Per D-02/D-03, create an IAM policy allowing exactly `s3:GetObject`, `s3:PutObject`, and `s3:DeleteObject` on `arn:aws:s3:::family-pickem/family-logos/*`, without ACL, bucket policy, public access, bucket wildcard, or unrelated prefix permissions. Create a parameterized idempotent provisioner for dev/prd that verifies bucket region, all Block Public Access flags, BucketOwnerEnforced ownership, encryption, and no public policy before creating/updating `family-pickem-<env>-logo-storage` and its policy. For access keys, first read only the dedicated Secrets Manager secret metadata/current key id and list IAM key metadata: reuse its currently Active matching key when present. Otherwise create at most one replacement key, hold its secret only in process memory, atomically `put-secret-value` the complete FAMILY_LOGO_* JSON without echoing or writing plaintext, force/verify ESO sync and migrated/web rollout using the new Secret version, then deactivate and delete the superseded key. If Secret Manager update, ESO sync, or workload verification fails, retain/reactivate the prior active key, do not delete it, revoke/delete the unadopted replacement, and exit nonzero with redacted key ids/version ids only. Write no key material to shell history, files, logs, Git, chart values, or manual Kubernetes Secrets. Fail closed on unsafe preflight or failed IAM simulation; simulate intended prefix object allows and root/different-prefix/ACL/list/bucket-policy denials. Document the reuse/rotation sequence, rollback, redacted identity/key/secret-version checks, and ESO rollout evidence.</action>
  <verify><automated>bash -n infra/family-logo-storage/provision.sh &amp;&amp; python -m json.tool infra/family-logo-storage/iam-policy.json &gt; /dev/null &amp;&amp; rg -n 'family-logos/\*|s3:GetObject|s3:PutObject|s3:DeleteObject' infra/family-logo-storage/iam-policy.json &amp;&amp; ! rg -n 'PutObjectAcl|PutBucketPolicy|public-read|AWS_ACCESS_KEY_ID|AWS_SECRET_ACCESS_KEY' infra/family-logo-storage/iam-policy.json docs/runbooks/family-logo-storage.md</automated></verify>
  <done>Authorized execution can provision/revoke a constrained identity and ESO-delivered secret while proving bucket and IAM posture without retaining secrets in repository artifacts.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-05 | Elevation | IAM policy | high | mitigate | D-02 exact object-prefix actions plus allow/deny simulation. |
| T-08-06 | Information disclosure | credentials | high | mitigate | D-03 Secrets Manager/ESO only; no Git/chart/manual Secret/CLI echo. |
| T-08-07 | Tampering | bucket controls | high | mitigate | Provisioner aborts if BPA, ownership, encryption, or policy preflight is unsafe. |
| T-08-08 | Elevation | pod injection | high | mitigate | Separate target reaches only web/migrate and Helm test excludes backup. |
</threat_model>
<verification>Render default/dev/prd charts, validate shell/JSON, then execute provisioner and preserve redacted AWS/ESO evidence.</verification>
<success_criteria>A revocable logo-only identity reaches only necessary pods through the existing managed secret chain and cannot expose or broaden the private bucket.</success_criteria>
<output>Create `.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-02-SUMMARY.md` when done.</output>
