---
phase: 08-private-aws-delivery-and-adversarial-verification
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - docs/runbooks/family-logo-storage.md
  - .planning/phases/08-private-aws-delivery-and-adversarial-verification/08-VALIDATION.md
autonomous: false
requirements: [S3-02, S3-03, SAFE-02, SAFE-03]
must_haves:
  truths:
    - "Automated Django, Helm, AWS policy/bucket, and ESO gates pass before any smoke is accepted."
    - "Staging then production upload/replace/remove proves short-lived private signed delivery, audit records, cleanup, and fallback."
    - "The GitHub Release workflow is not started until evidence is complete and the public Helm chart availability gate is satisfied."
  artifacts:
    - path: .planning/phases/08-private-aws-delivery-and-adversarial-verification/08-VALIDATION.md
      provides: "Requirement-to-command release evidence"
    - path: docs/runbooks/family-logo-storage.md
      provides: "Completed redacted staging/production smoke record"
  key_links:
    - "Plan 01/02 controls -> staging smoke -> production smoke -> GitHub Release gate"
---

<objective>
Execute D-05's automated and deployed verification gate before releasing family logo uploads.

Purpose: connect adversarial tests and infrastructure controls to observed staging and production lifecycle behavior.
Output: validation evidence, smoke record, and explicit ship/no-ship determination.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-CONTEXT.md
@.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-RESEARCH.md
@.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-01-SUMMARY.md
@.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-02-SUMMARY.md
@docs/runbooks/family-logo-storage.md
@AGENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Execute and record automated cloud and application release gates</name>
  <files>docs/runbooks/family-logo-storage.md, .planning/phases/08-private-aws-delivery-and-adversarial-verification/08-VALIDATION.md</files>
  <action>Per D-05, run and record focused storage/settings hostile-lifecycle tests, full Django suite, Django check, migration dry run, default/dev/prd Helm renders, JSON/shell validation, provisioner read-only bucket checks, IAM allow/deny simulation, and ESO target Ready/workload-injection checks. Record timestamp, test counts, redacted ARNs/key names/secret versions, release/image/chart versions, and result for every requirement. Stop immediately on a test failure, public/unsafe bucket posture, unexpected principal permission, plaintext secret, wrong workload injection, or missing ESO sync; do not run a smoke or publish a release after a failed automated gate.</action>
  <verify><automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_api.tests.test_family_logo_storage pickem_homepage.tests.FamilyLogoUploadFoundationTests --settings=pickem.test_settings --verbosity=2 &amp;&amp; ../venv/bin/python manage.py test --settings=pickem.test_settings --verbosity=1 &amp;&amp; ../venv/bin/python manage.py check --settings=pickem.test_settings &amp;&amp; ../venv/bin/python manage.py makemigrations --check --dry-run --settings=pickem.test_settings &amp;&amp; helm template family-pickem charts/family-pickem &gt; /tmp/fp-default.yaml &amp;&amp; helm template family-pickem-dev charts/family-pickem -f infra/app/values-dev.yaml &gt; /tmp/fp-dev.yaml &amp;&amp; helm template family-pickem-prd charts/family-pickem -f infra/app/values-prd.yaml &gt; /tmp/fp-prd.yaml &amp;&amp; bash -n infra/family-logo-storage/provision.sh &amp;&amp; python -m json.tool infra/family-logo-storage/iam-policy.json &gt; /dev/null</automated></verify>
  <done>The validation record proves every automated gate passes with redacted evidence or explicitly records the blocking failure and no-ship result.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve staging then production upload lifecycle smoke</name>
  <files>docs/runbooks/family-logo-storage.md, .planning/phases/08-private-aws-delivery-and-adversarial-verification/08-VALIDATION.md</files>
  <action>Perform D-05's final deployed verification only after Task 1 passes. Record the redacted results in the runbook and validation artifact; a failed step is a no-ship result requiring remediation before repeat verification.</action>
  <what-built>Private signed delivery, prefix-limited IAM/ESO credentials, adversarial tests, and post-commit old-object cleanup are deployed after passing Task 1 gates.</what-built>
  <how-to-verify>
    1. As a commissioner of a disposable staging family, upload a small JPEG/PNG/WebP at the existing settings URL; confirm family surfaces use a short-lived S3 signed URL, not a public or app-proxy URL.
    2. Replace with a different image, then remove it; confirm new mark then default fallback, no original delivery, and redacted audit/object evidence shows each old generated key is deleted only after its committed replacement/removal.
    3. Repeat the bounded upload/replace/remove sequence in production only after staging passes. Record environment, operator, time, chart/image version, object-key hash, audit id, observed expiry, and cleanup result in the runbook without credentials or full signed URL.
    4. Confirm all evidence is complete before publishing the GitHub Release. After publication, verify the required Helm chart version appears in the public Helm repository before advancing ArgoCD.
  </how-to-verify>
  <resume-signal>Type "approved" after staging and production smoke records are complete, or describe the failed step.</resume-signal>
  <verify><automated>test -s docs/runbooks/family-logo-storage.md &amp;&amp; test -s .planning/phases/08-private-aws-delivery-and-adversarial-verification/08-VALIDATION.md</automated><human-check>Complete every numbered staging and production smoke step and record redacted evidence before approving.</human-check></verify>
  <done>Staging and production lifecycle smoke records demonstrate signed delivery, audit, cleanup, and fallback; otherwise release remains blocked.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-09 | Tampering | deployment drift | high | mitigate | D-05 requires renders, ESO, IAM, and bucket checks before smoke/release. |
| T-08-10 | Information disclosure | deployed image URL | high | mitigate | Smoke accepts only short-lived signed reads and redacts evidence. |
| T-08-11 | Repudiation | production verification | medium | mitigate | Runbook captures operator, versions, audits, and key hashes. |
| T-08-12 | Denial | stale cleanup | medium | mitigate | Smoke validates cleanup; a failure blocks release and follows remediation runbook. |
</threat_model>
<verification>Completion requires the automated commands and the blocking staging then production smoke checkpoint. A GitHub Release, not a tag alone, is the production publish trigger.</verification>
<success_criteria>Evidence proves the private lifecycle works in both environments and release cannot bypass its security/delivery gate.</success_criteria>
<output>Create `.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-03-SUMMARY.md` when done.</output>
