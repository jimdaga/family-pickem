---
phase: 08-private-aws-delivery-and-adversarial-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pickem/pickem/settings.py
  - pickem/pickem_api/storage.py
  - pickem/pickem_api/tests/test_family_logo_storage.py
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements: [S3-02, SAFE-02, SAFE-03]
must_haves:
  truths:
    - "Configured logo storage uses only dedicated FAMILY_LOGO_* credentials and emits 300-second signed GET URLs for controlled family-logos/ objects."
    - "Absent logo configuration preserves local media storage; partial configuration fails closed instead of falling back to generic AWS credentials."
    - "Denied tenant, forged-key, and CSRF-less mutations produce no logo, storage, or audit side effects."
    - "Successful replace/remove deletes exactly the old generated key after commit; failed DB/audit work compensates only the new key."
  artifacts:
    - path: pickem/pickem_api/storage.py
      provides: "Dedicated S3 configuration and bounded signed URLs"
    - path: pickem/pickem_homepage/views.py
      provides: "Post-commit obsolete object cleanup"
    - path: pickem/pickem_homepage/tests.py
      provides: "Adversarial authorization and lifecycle tests"
  key_links:
    - "FAMILY_LOGO_* -> FamilyLogoStorage -> private S3 signed URL"
    - "locked Family/audit transaction -> on_commit captured old-key delete"
---

<objective>
Implement D-01 and D-04 in the application: dedicated private logo storage configuration, complete hostile-request coverage, and transaction-safe old-object cleanup.

Purpose: satisfy S3-02, SAFE-02, and SAFE-03 without changing global static/backup credentials.
Output: dedicated settings/storage, lifecycle implementation, and isolated Django regressions.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-CONTEXT.md
@.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-RESEARCH.md
@.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-PATTERNS.md
@pickem/pickem_api/storage.py
@pickem/pickem/settings.py
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/tests.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Bind FamilyLogoStorage to dedicated logo-only configuration</name>
  <files>pickem/pickem/settings.py, pickem/pickem_api/storage.py, pickem/pickem_api/tests/test_family_logo_storage.py</files>
  <behavior>
    - Complete FAMILY_LOGO_* settings initialize S3 using only logo bucket, region, key, and secret values.
    - Missing all logo settings uses local media without network calls; a partial set is a configuration error.
    - Generated object URLs use a fixed 300-second expiry while retaining private WebP parameters and family-logos/ prefix.
  </behavior>
  <action>Per D-01/D-02, add an all-or-none `FAMILY_LOGO_STORAGE_BUCKET_NAME`, `FAMILY_LOGO_AWS_S3_REGION_NAME`, `FAMILY_LOGO_AWS_ACCESS_KEY_ID`, `FAMILY_LOGO_AWS_SECRET_ACCESS_KEY`, and validated `FAMILY_LOGO_AWS_QUERYSTRING_EXPIRE` contract adjacent to the generic AWS block. Make `FamilyLogoStorage` use only those values when configured; retain filesystem delegation only when all are absent, preserve `location`, private ACL, WebP parameters, and generated names, and default the signing expiry to 300 seconds. Reject incomplete values without inspecting generic AWS credentials. Add override-settings/mock tests proving constructor inputs, no live calls, signed expiry, and local fallback. Do not change generic static/default storage or backup credentials.</action>
  <verify><automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_api.tests.test_family_logo_storage --settings=pickem.test_settings --verbosity=2 &amp;&amp; ../venv/bin/python manage.py check --settings=pickem.test_settings</automated></verify>
  <done>Logo storage is independently configured, privately signed for five minutes, and cannot silently inherit broad generic credentials.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clean obsolete objects only after successful locked mutations</name>
  <files>pickem/pickem_homepage/views.py, pickem/pickem_homepage/tests.py</files>
  <behavior>
    - First create performs no old-key deletion; replace/remove delete exactly the captured old key after commit.
    - DB/audit failure deletes only the new key and preserves the old database reference.
    - Anonymous, member, wrong-family/pool, forged family/pool/key, and missing-CSRF POSTs have no DB, storage, or audit effects.
  </behavior>
  <action>Per D-04, capture the current generated name before replacing or clearing the locked route-derived Family logo. Retain Phase 6's new-write-before-row/audit compensation exactly. Once both row and audit writes succeed, register `transaction.on_commit` with captured defaults to delete only that obsolete key; do not register a callback on first upload or failed transaction. Add a narrow deletion helper that logs stable family/key correlation on S3 deletion failure without URLs, credentials, filename, or source metadata. Preserve existing tenant decorator, CSRF, locks, audit shape, and rejection semantics; never accept posted identity/object fields or list storage. Expand isolated real-route tests/spies for the SAFE-02 and SAFE-03 matrices and Django TestCase callback execution.</action>
  <verify><automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyLogoUploadFoundationTests --settings=pickem.test_settings --verbosity=2 &amp;&amp; ../venv/bin/python manage.py test pickem_homepage pickem_api --settings=pickem.test_settings --verbosity=1</automated></verify>
  <done>Committed replacements/removals clean only obsolete generated keys; all hostile or failed paths retain the referenced asset and have no cross-family effects.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01 | Elevation | storage config | high | mitigate | Dedicated FAMILY_LOGO_* values are required and generic credential fallback is tested absent. |
| T-08-02 | Tampering | settings POST/key | high | mitigate | D-04 keeps tenant locks/CSRF and proves forged fields have no side effects. |
| T-08-03 | Repudiation | lifecycle | medium | mitigate | Audit remains transactional; post-commit deletion failures log stable correlation only. |
| T-08-04 | Information disclosure | delivery | medium | mitigate | D-01 uses short signed URLs for server-controlled names only. |
</threat_model>

<verification>Run focused storage and settings tests, app suites, and Django checks with local fake storage only.</verification>
<success_criteria>Dedicated private delivery and exact post-commit cleanup withstand tenant, CSRF, object-key, and persistence attacks.</success_criteria>
<output>Create `.planning/phases/08-private-aws-delivery-and-adversarial-verification/08-01-SUMMARY.md` when done.</output>
