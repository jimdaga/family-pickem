---
phase: 05-family-admin-experience
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - pickem/pickem_homepage/forms.py
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/urls.py
  - pickem/pickem_homepage/templates/pickem/family_admin_settings.html
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements: [AUTHZ-03, AUTHZ-05, POOL-04, COMM-03, SEC-01]
must_haves:
  truths:
    - "D-12 Owners/admins can edit basic current-family, current-pool, and represented rules/settings values."
    - "D-13 Settings writes are tenant-scoped, validate server-resolved family/pool, and create audit logs."
    - "D-14 No broad scoring-rule model redesign is introduced."
    - "COMM-03 Existing family banner data is not leaked or globally mutated from this settings surface."
  artifacts:
    - "pickem/pickem_homepage/templates/pickem/family_admin_settings.html"
    - "Admin settings forms in pickem/pickem_homepage/forms.py"
  key_links:
    - "settings POST -> request.tenant_context -> Family/Pool/PoolSettings scoped update -> FamilyAuditLog"
    - "settings page/tests -> current-family banner isolation assertions -> no SiteBanner mutation/deactivation"
---

<objective>
Add tenant-scoped family, pool, and represented rules/settings editing per D-12, D-13, D-14, D-21, D-22, D-23, D-26, D-27, and D-28.

Purpose: Replace global/basic commissioner settings behavior with scoped owner/admin operations.
Output: Settings forms, route, template, audit logging, and negative cross-family tests.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-family-admin-experience/05-CONTEXT.md
@.planning/phases/05-family-admin-experience/05-RESEARCH.md
@.planning/phases/05-family-admin-experience/05-PATTERNS.md
@.planning/phases/05-family-admin-experience/05-01-SUMMARY.md
@pickem/pickem_api/models.py
@pickem/pickem_homepage/models.py
@pickem/pickem_homepage/forms.py
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/templates/pickem/rules.html
@pickem/pickem_homepage/tests.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add settings authorization and scoping tests</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <behavior>
    - Admin and owner can update current-family display name, current-pool display name, `PoolSettings.picks_lock_at_kickoff`, and `PoolSettings.allow_tiebreaker` per D-12.
    - POSTed family_id, pool_id, or unrelated object IDs cannot target another family/pool per AUTHZ-05 and D-28.
    - Member, outsider, inactive, and anonymous users cannot mutate settings per D-26 and D-28.
    - Settings mutations create current-family `FamilyAuditLog` entries per D-13 and SEC-01.
    - The settings page does not render another family's banner metadata and does not deactivate any `SiteBanner` rows per COMM-03.
  </behavior>
  <action>Add RED tests in `FamilyAdminExperienceTests` for settings GET/POST, forged body IDs, banner metadata non-leakage, CSRF-enforced POSTs, and audit metadata without secrets. Preserve dirty work and avoid touching the ongoing model/logo migration unless implementation proves it is required.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>Tests fail on missing settings route/forms or missing mutations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement scoped settings forms and mutations</name>
  <files>pickem/pickem_homepage/forms.py, pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_homepage/templates/pickem/family_admin_settings.html</files>
  <action>Create Tailwind-styled command forms for basic family name, pool name, and represented `PoolSettings` booleans. Add a tenant settings route under the admin hub using `family_member_required(admin)` per D-26. Update only `request.tenant_context.family`, `request.tenant_context.pool`, and that pool's settings row; do not add banner editing, do not deactivate any `SiteBanner` rows, and do not rely on the current dirty `Family.logo_url` field. Wrap writes in `transaction.atomic()` and create `FamilyAuditLog.Action.POOL_SETTINGS_UPDATED` as the Phase 5 generic settings audit action with metadata naming `target_type`, changed fields, target ids, and before/after values that do not contain raw secrets. Migrations: none expected.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>Settings mutations affect only the current tenant and create scoped audit entries.</done>
</task>

<task type="auto">
  <name>Task 3: Wire admin hub/settings navigation and manual QA</name>
  <files>pickem/pickem_homepage/templates/pickem/family_admin.html, pickem/pickem_homepage/templates/pickem/family_admin_settings.html, pickem/pickem_homepage/tests.py</files>
  <action>Link the settings section from the admin hub and provide practical empty/error states per D-21 and D-22. Add assertions that the settings page preserves visible current family/pool context and does not include another family's banner metadata. Keep template changes targeted to Phase 5 admin surfaces per D-23.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py makemigrations --check --dry-run --settings=pickem.test_settings</automated>
    <manual>Visit `/families/&lt;family_slug&gt;/pools/&lt;pool_slug&gt;/admin/settings/` as an owner/admin and confirm the forms render, save, and return visible success/error feedback.</manual>
  </verify>
  <done>Admin settings page is reachable from the hub, tested, and has no pending migrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser POST -> settings view | Untrusted form fields request tenant settings mutations. |
| settings view -> audit log | Sensitive administrative state changes are recorded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | settings POST handlers | high | mitigate | Ignore forged tenant identifiers and mutate only `request.tenant_context` objects. |
| T-05-04 | Information Disclosure | family banner data | medium | mitigate | Do not expose or mutate banner rows from settings; test other-family banner exclusion. |
| T-05-05 | Repudiation | settings changes | medium | mitigate | Create `FamilyAuditLog` entries inside the same transaction as each write. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are planned; if introduced, stop for package legitimacy review. |
</threat_model>

<verification>
Focused tests, migration dry-run, Django check, `git diff --check`, and manual settings-page QA on the running local server. Rollback: revert the settings route/forms/template hunks; no schema rollback should be required.
</verification>

<success_criteria>
Owners/admins can safely edit basic family/pool/settings data for the current tenant, with audit logs and cross-family negative tests.
</success_criteria>

<output>
Create `.planning/phases/05-family-admin-experience/05-02-SUMMARY.md` when done.
</output>
