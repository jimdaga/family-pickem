---
phase: 05-family-admin-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pickem/pickem_homepage/urls.py
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/templates/pickem/family_admin.html
  - pickem/pickem_homepage/templates/pickem/base.html
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements: [AUTHZ-03, AUTHZ-05, SEC-01]
must_haves:
  truths:
    - "D-01 D-03 Owners/admins have a dedicated tenant admin hub under `/families/<family_slug>/pools/<pool_slug>/admin/`."
    - "D-02 D-26 Members do not see the admin navigation affordance and cannot access the hub by direct URL."
    - "D-19 D-20 Recent audit entries shown in the hub are scoped to the current family and hidden from members."
  artifacts:
    - "pickem/pickem_homepage/templates/pickem/family_admin.html"
    - "pickem/pickem_homepage/tests.py::FamilyAdminExperienceTests"
  key_links:
    - "tenant URL -> family_member_required(minimum_role=FamilyMembership.Role.ADMIN) -> request.tenant_context -> scoped hub queries"
    - "base navigation -> current_membership role check -> tenant admin URL"
---

<objective>
Create the Phase 5 tenant admin hub shell and authorization baseline per D-01, D-02, D-03, D-19, D-20, D-21, D-22, D-23, D-26, and D-28.

Purpose: Establish the explicit family/pool admin surface before adding mutations.
Output: Tenant admin routes, hub template, scoped recent-audit display, navigation affordance, and negative authorization tests.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-family-admin-experience/05-CONTEXT.md
@.planning/phases/05-family-admin-experience/05-RESEARCH.md
@.planning/phases/05-family-admin-experience/05-PATTERNS.md
@.planning/phases/04-family-scoped-app-pages/04-06-SUMMARY.md
@pickem/pickem_homepage/authz.py
@pickem/pickem_api/authz.py
@pickem/pickem_homepage/urls.py
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/templates/pickem/base.html
@pickem/pickem_homepage/tests.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing admin hub authorization coverage</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <behavior>
    - Anonymous browser GET to the tenant admin hub redirects to login per D-26.
    - Authenticated outsider and inactive membership GETs raise 404 per D-26 and D-28.
    - Active member GET returns 403 per D-26 and D-28.
    - Active admin and owner GETs render only current-family audit rows per D-19 and D-20.
    - Forged family/pool slug combinations cannot render another family's hub per D-28.
  </behavior>
  <action>Create `FamilyAdminExperienceTests` fixtures with owner, admin, member, inactive user, outsider, a second family/pool, and scoped `FamilyAuditLog` rows. Write RED tests for the admin hub route and base navigation affordance before implementation. Preserve dirty work: inspect any touched dirty file and stage only Phase 5 hunks.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>Tests fail because the tenant admin route/template/navigation do not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tenant admin hub route and scoped audit display</name>
  <files>pickem/pickem_homepage/urls.py, pickem/pickem_homepage/views.py, pickem/pickem_homepage/templates/pickem/family_admin.html</files>
  <action>Add `family_pool_admin` at `/families/&lt;family_slug&gt;/pools/&lt;pool_slug&gt;/admin/` per D-01 using `@family_member_required(minimum_role=FamilyMembership.Role.ADMIN)` per D-26. In the view, derive `family`, `pool`, and `membership` from `request.tenant_context`, query recent `FamilyAuditLog` rows by current family only, and pass section URLs/context for later admin pages. Build a polished Tailwind-aligned operational template per D-21 and D-22 with current family/pool context, links or disabled sections for settings, members, invites, picks, winners, and a recent-audit table. Do not add package installs or migrations.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>Admin/owner can load the hub, member/outsider/inactive users cannot, and other-family audit rows never render.</done>
</task>

<task type="auto">
  <name>Task 3: Add tenant-role navigation affordance</name>
  <files>pickem/pickem_homepage/templates/pickem/base.html, pickem/pickem_homepage/tests.py</files>
  <action>Replace or supplement the legacy commissioner navigation affordance with a tenant admin link only when `current_family`, `current_pool`, and `current_membership.role` indicate owner/admin per D-02. The link must point to `family_pool_admin`; members must not see it. Keep server-side hub authorization authoritative and avoid broad base-template visual churn per D-23.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
    <manual>With the dev server already running, `curl -s --max-time 5 http://localhost:8000 | head -40` should still return public homepage HTML.</manual>
  </verify>
  <done>Authorized tenant admins see a tenant admin link in navigation; members do not; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> tenant admin route | Untrusted slugs cross into tenant context resolution. |
| template context -> audit log display | Private audit data is rendered to browser users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `family_pool_admin` audit query | high | mitigate | Scope `FamilyAuditLog` query to `request.tenant_context.family` and test cross-family rows. |
| T-05-02 | Elevation of Privilege | admin hub route | high | mitigate | Use `family_member_required(admin)` and negative tests for member, outsider, inactive, anonymous, and slug tampering. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are planned; if introduced, stop for package legitimacy review. |
</threat_model>

<verification>
Run focused tests, `cd pickem &amp;&amp; ../venv/bin/python manage.py check --settings=pickem.test_settings`, and inspect `git diff --check`. Manual QA: admin and owner can open the hub; member gets 403; outsider gets 404; public home still renders.
</verification>

<success_criteria>
Tenant admin hub exists, is discoverable only for authorized tenant admins, displays scoped recent audit logs, and preserves unrelated dirty frontend work.
</success_criteria>

<output>
Create `.planning/phases/05-family-admin-experience/05-01-SUMMARY.md` when done.
</output>
