---
phase: 05-family-admin-experience
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - pickem/pickem_homepage/forms.py
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/urls.py
  - pickem/pickem_homepage/templates/pickem/family_admin_members.html
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements: [AUTHZ-03, AUTHZ-05, SEC-01]
must_haves:
  truths:
    - "D-04 Authorized admins can view members, while owners can perform role/status changes."
    - "D-05 Owner-sensitive role/status actions require owner role."
    - "D-06 The last active owner cannot be demoted, deactivated, or removed."
    - "D-07 Role/status changes are enforced server-side, not by hidden UI controls."
  artifacts:
    - "pickem/pickem_homepage/templates/pickem/family_admin_members.html"
    - "Membership command form(s) in pickem/pickem_homepage/forms.py"
  key_links:
    - "owner POST -> current-family membership lookup -> last-owner check -> membership update -> audit log"
---

<objective>
Implement tenant member, role, status, and owner-safety management per D-04 through D-07, D-21, D-22, D-23, D-26, D-27, and D-28.

Purpose: Replace global commissioner-style user control with family-scoped least-privilege membership management.
Output: Member admin route/template/forms, last-owner protection, audit logs, and negative tests.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-family-admin-experience/05-CONTEXT.md
@.planning/phases/05-family-admin-experience/05-RESEARCH.md
@.planning/phases/05-family-admin-experience/05-PATTERNS.md
@.planning/phases/05-family-admin-experience/05-02-SUMMARY.md
@pickem/pickem_api/authz.py
@pickem/pickem_api/models.py
@pickem/pickem_homepage/forms.py
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/tests.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add membership management negative tests</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <behavior>
    - Member list shows only active/inactive memberships for the current family and never another family.
    - Owners can change eligible roles/statuses; admins cannot promote, demote, transfer ownership, deactivate, or reactivate memberships per D-05 and D-27.
    - Last active owner cannot be demoted or deactivated per D-06.
    - Forged membership IDs and user IDs from another family return non-leaking denial and do not mutate rows per AUTHZ-05 and D-28.
    - Every successful role/status mutation creates `MEMBERSHIP_UPDATED` audit metadata with previous and new values per SEC-01.
  </behavior>
  <action>Add RED tests for member listing, owner-only mutations, last-owner safety, outsider/member/admin denial, inactive actor denial, forged cross-family membership IDs, and CSRF-enforced POSTs. Keep tests in the existing test module but isolate fixtures under `FamilyAdminExperienceTests`.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>Tests fail until member management routes/forms exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement owner-protected membership mutations</name>
  <files>pickem/pickem_homepage/forms.py, pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py</files>
  <action>Add member list and membership update routes under the tenant admin hub. Use `family_member_required(admin)` for list viewing per D-04 and an explicit owner-role check for all role/status mutation POSTs per D-05 and D-27. Resolve target memberships with `FamilyMembership.objects.select_for_update().get(family=request.tenant_context.family, id=...)`. Before demoting/deactivating an owner, count active owners within the same transaction and reject changes that would lose the last active owner per D-06. Reactivation must remain current-family scoped. Create `FamilyAuditLog.Action.MEMBERSHIP_UPDATED` rows with target membership id, target user id, previous/new role, previous/new status, and actor id. Migrations: none.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>Role/status updates are current-family scoped, owner-protected, last-owner safe, and audit logged.</done>
</task>

<task type="auto">
  <name>Task 3: Render practical member management UI</name>
  <files>pickem/pickem_homepage/templates/pickem/family_admin.html, pickem/pickem_homepage/templates/pickem/family_admin_members.html, pickem/pickem_homepage/tests.py</files>
  <action>Create a mobile-friendly members table/cards view per D-21 and D-22 with current family/pool context, role/status chips, owner-only action controls, and clear disabled states. Do not depend on hidden controls for authorization; tests must continue to POST forbidden actions directly. Link the page from the admin hub.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
    <manual>Visit the members page as owner, admin, and member; confirm owner sees mutation controls, admin sees operational member data without owner-sensitive controls, and member receives 403.</manual>
  </verify>
  <done>Members page is usable, scoped, and protected by automated direct-POST negative tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser POST -> membership update | Untrusted role/status and membership IDs request privilege changes. |
| membership update -> family ownership invariant | A request can remove the only active owner if unchecked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege | membership update view | critical | mitigate | Owner-only mutation checks plus forged ID tests. |
| T-05-07 | Denial of Service | last-owner demotion/deactivation | high | mitigate | Transactional active-owner count before owner role/status changes. |
| T-05-08 | Repudiation | membership changes | medium | mitigate | Audit each successful mutation with before/after metadata. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are planned; if introduced, stop for package legitimacy review. |
</threat_model>

<verification>
Focused tests, Django check, `git diff --check`, and manual owner/admin/member role QA. Rollback: revert member routes/forms/template hunks; no migrations.
</verification>

<success_criteria>
Owners can manage family memberships safely; admins and members cannot exceed least privilege; last-owner protection is enforced and tested.
</success_criteria>

<output>
Create `.planning/phases/05-family-admin-experience/05-03-SUMMARY.md` when done.
</output>
