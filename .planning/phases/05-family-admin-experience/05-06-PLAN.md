---
phase: 05-family-admin-experience
plan: 06
type: execute
wave: 6
depends_on: [05-05]
files_modified:
  - pickem/pickem_homepage/forms.py
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/urls.py
  - pickem/pickem_homepage/templates/pickem/family_admin_winners.html
  - pickem/pickem_homepage/templates/pickem/family_admin.html
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements: [AUTHZ-03, AUTHZ-05, SEC-01]
must_haves:
  truths:
    - "D-15 Existing commissioner week-winner/bonus selection is available in tenant admin form."
    - "D-16 Winner actions validate week, user, and standing rows against current pool/family."
    - "D-17 Week numbers are validated before dynamic week fields are constructed."
    - "D-18 Winner overrides create audit log entries with safe metadata."
    - "D-24 D-25 Legacy global commissioner routes are disabled after tenant replacements exist."
    - "D-26 Any JSON/fetch winner or legacy-denial handlers return JSON auth/404/403 denials instead of browser redirects."
  artifacts:
    - "pickem/pickem_homepage/templates/pickem/family_admin_winners.html"
    - "Legacy commissioner route denial tests in pickem/pickem_homepage/tests.py"
  key_links:
    - "week number -> 1..18 validation -> dynamic userSeasonPoints fields"
    - "winner uid -> active current-family membership -> current-pool userSeasonPoints row"
    - "legacy commissioner URLs -> denial response instead of global admin rendering/mutation"
---

<objective>
Migrate week-winner administration and disable legacy global commissioner routes per D-15 through D-18 and D-24 through D-28.

Purpose: Finish tenant replacements for score-affecting admin workflows and close the global commissioner path.
Output: Tenant winner admin page/handler, invalid-week protection, audit logging, and legacy denial tests.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-family-admin-experience/05-CONTEXT.md
@.planning/phases/05-family-admin-experience/05-RESEARCH.md
@.planning/phases/05-family-admin-experience/05-PATTERNS.md
@.planning/phases/05-family-admin-experience/05-05-SUMMARY.md
@pickem/pickem_api/models.py
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/templates/pickem/commissioners.html
@pickem/pickem_homepage/tests.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add week-winner and legacy denial tests</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <behavior>
    - Admin/owner can view candidates and set weekly winner/bonus for current-pool rows only per D-15 and D-16.
    - Invalid week values below 1, above 18, non-integers, and malformed JSON/form values are rejected before dynamic field names are built per D-17.
    - Forged winner user IDs and season/pool/body fields cannot mutate another family/pool per AUTHZ-05 and D-28.
    - Successful winner override creates `WEEK_WINNER_UPDATED` audit metadata per D-18.
    - Legacy `/commissioners/` and related mutation/retrieval routes no longer render or mutate global commissioner tools per D-24 and D-25.
    - If winner administration uses JSON/fetch endpoints, anonymous JSON requests return an auth error instead of a login redirect, non-members receive non-leaking JSON 404, active members without admin role receive JSON 403, inactive memberships are denied, and response bodies do not leak family/pool existence per D-26.
  </behavior>
  <action>Add RED tests for tenant winner candidate display, set winner, invalid week values, forged user/body IDs, member/outsider/inactive denial, CSRF enforcement, audit logging, and legacy route denial. Include winner JSON/fetch denial tests if JSON is used, plus legacy JSON route expectations that return non-leaking denial JSON or appropriate 404/403/auth responses with no login redirect body.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>Tests fail until tenant winner route and legacy denials exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pool-scoped week-winner administration</name>
  <files>pickem/pickem_homepage/forms.py, pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_homepage/templates/pickem/family_admin_winners.html</files>
  <action>Add winner admin routes under `/admin/winners/` using `family_member_required(admin)` for browser pages per D-26. For any JSON/fetch candidate or mutation route, add JSON-aware authorization handling that preserves the Phase 2 denial split: anonymous requests return an auth error status/body without redirecting to login, authenticated non-members return non-leaking 404, active members below admin return 403, and inactive memberships are denied. Validate `week_number` as an integer in 1..18 before constructing `week_{n}_winner`, `week_{n}_bonus`, or related dynamic fields per D-17. Compute candidates from `GamePicks` filtered by `pool=request.tenant_context.pool`, selected week, season, and competition. Resolve winner through active current-family membership and a `userSeasonPoints` row filtered by current pool and season. Update only current-pool rows, recalculate totals consistently with existing legacy behavior, and create `FamilyAuditLog.Action.WEEK_WINNER_UPDATED` with previous/new winner, week, bonus context, and actor id. Do not harden background cron/scoring jobs in this plan; Phase 6 owns that separate operational path.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>Week-winner updates are current-pool scoped, invalid weeks are rejected safely, and audit logs are written.</done>
</task>

<task type="auto">
  <name>Task 3: Disable legacy global commissioner surfaces</name>
  <files>pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py, pickem/pickem_homepage/templates/pickem/base.html, pickem/pickem_homepage/tests.py</files>
  <action>After tenant replacements exist, change legacy global commissioner page and JSON endpoints to deny access instead of rendering/mutating global tools per D-24 and D-25. Use 404/403/generic JSON denial consistent with D-26 and route type; legacy JSON denials must not redirect to login HTML or disclose tenant/global data. Do not leave `commissioner_required` as a path to global mutation. Keep URL names only if existing templates/tests need graceful denial. Remove legacy global commissioner navigation affordances left after Plan 05-01 so authorized users use tenant admin URLs. Preserve dirty template work by staging only this denial/navigation hunk.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage pickem_api --settings=pickem.test_settings --verbosity=2</automated>
    <manual>Request `/commissioners/` and the old commissioner JSON URLs as a legacy commissioner/superuser and confirm they do not render or mutate global admin tools.</manual>
  </verify>
  <done>Legacy global commissioner surfaces are closed and tenant winner admin passes focused and app-level tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/JSON POST -> winner handler | Untrusted week/user/body fields request score-affecting updates; JSON clients must not receive browser redirect denials. |
| legacy commissioner URLs -> application | Old global admin paths remain reachable unless explicitly denied. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-15 | Tampering | week winner handler | high | mitigate | Validate week before dynamic fields and scope all rows by current pool. |
| T-05-16 | Elevation of Privilege | legacy commissioner routes | critical | mitigate | Deny legacy global commissioner page/JSON paths after tenant replacements ship. |
| T-05-17 | Repudiation | winner override | medium | mitigate | Audit every successful override with week and before/after metadata. |
| T-05-17A | Information Disclosure | JSON denial behavior | high | mitigate | Implement and test JSON-aware auth/404/403 denials with no redirect/login HTML and no tenant existence leakage. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are planned; if introduced, stop for package legitimacy review. |
</threat_model>

<verification>
Focused tests, app-level tests, Django check, `git diff --check`, and manual legacy-route denial QA. Rollback: restore legacy route denial hunk only if a tenant replacement regression is found, without changing Phase 6 cron scope.
</verification>

<success_criteria>
Tenant week-winner admin is available and safe, invalid week inputs are handled, and legacy global commissioner surfaces are disabled.
</success_criteria>

<output>
Create `.planning/phases/05-family-admin-experience/05-06-SUMMARY.md` when done.
</output>
