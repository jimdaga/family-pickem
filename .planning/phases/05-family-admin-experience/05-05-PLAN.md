---
phase: 05-family-admin-experience
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - pickem/pickem_homepage/forms.py
  - pickem/pickem_homepage/views.py
  - pickem/pickem_homepage/urls.py
  - pickem/pickem_homepage/templates/pickem/family_admin_picks.html
  - pickem/pickem_homepage/tests.py
autonomous: true
requirements: [AUTHZ-03, AUTHZ-05, SEC-01]
must_haves:
  truths:
    - "D-15 Existing commissioner manual pick submission and user-pick retrieval are available in tenant admin form."
    - "D-16 Manual pick and retrieval actions validate week/user/game against current family/pool."
    - "D-18 Manual pick mutations create audit log entries with useful safe metadata."
  artifacts:
    - "pickem/pickem_homepage/templates/pickem/family_admin_picks.html"
    - "Manual pick command forms in pickem/pickem_homepage/forms.py"
  key_links:
    - "target user id -> active current-family membership -> pool-scoped picks"
    - "game id/week -> current pool season/competition -> pool-scoped GamePicks update"
---

<objective>
Migrate manual pick submission and user-pick retrieval into the tenant admin hub per D-15, D-16, D-18, D-21, D-22, D-23, D-26, D-27, and D-28.

Purpose: Replace global commissioner pick tools with pool-scoped admin actions.
Output: Tenant manual-pick page/JSON handlers, scoped retrieval, audit logs, and negative IDOR tests.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-family-admin-experience/05-CONTEXT.md
@.planning/phases/05-family-admin-experience/05-RESEARCH.md
@.planning/phases/05-family-admin-experience/05-PATTERNS.md
@.planning/phases/05-family-admin-experience/05-04-SUMMARY.md
@pickem/pickem_api/models.py
@pickem/pickem_homepage/forms.py
@pickem/pickem_homepage/views.py
@pickem/pickem_homepage/templates/pickem/commissioners.html
@pickem/pickem_homepage/tests.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add manual pick tenant-scope tests</name>
  <files>pickem/pickem_homepage/tests.py</files>
  <behavior>
    - Admin and owner can retrieve current-pool picks for active current-family users only per D-15 and D-16.
    - Manual pick submission creates or updates `GamePicks` with `pool=request.tenant_context.pool` and server-derived user/game/season/week fields per D-16.
    - Forged user IDs, game IDs, pool IDs, season, week, competition, correctness, and pick IDs cannot cross tenant boundaries per AUTHZ-05 and D-28.
    - Members, outsiders, inactive memberships, anonymous users, and CSRF-missing POSTs cannot read or mutate manual pick data per D-26 and D-28.
    - Successful manual pick changes create `MANUAL_PICK_UPDATED` audit rows per D-18.
  </behavior>
  <action>Add RED tests for tenant user-pick retrieval, manual pick creation/update, invalid team selection, cross-family user and game tampering, forged body fields, member/outsider/inactive denial, and CSRF enforcement. Include tests for both form-page and JSON/fetch behavior if the implementation uses JSON.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>Tests fail until tenant manual-pick routes and handlers exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pool-scoped manual pick retrieval and mutation</name>
  <files>pickem/pickem_homepage/forms.py, pickem/pickem_homepage/views.py, pickem/pickem_homepage/urls.py</files>
  <action>Add admin picks routes under `/admin/picks/` using `family_member_required(admin)` per D-26. Validate target users through active `FamilyMembership` rows in `request.tenant_context.family`. Validate games through `GamesAndScores` filtered by `tenant_context.pool.season`, `tenant_context.pool.competition`, and submitted week; selected pick must be the game's home or away slug. Use the Phase 4 pool/user/game pick identity convention and set `pool=tenant_context.pool`; ignore any forged user, pool, season, week, competition, or correctness fields. Create `FamilyAuditLog.Action.MANUAL_PICK_UPDATED` with previous pick, new pick, target user id, game id, week, and actor id. Do not copy `@csrf_exempt` from legacy commissioner views. Migrations: none.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>Manual pick reads/writes are current-pool scoped and audit logged.</done>
</task>

<task type="auto">
  <name>Task 3: Render manual pick admin UI</name>
  <files>pickem/pickem_homepage/templates/pickem/family_admin.html, pickem/pickem_homepage/templates/pickem/family_admin_picks.html, pickem/pickem_homepage/tests.py</files>
  <action>Create an operational Tailwind admin picks page per D-21 and D-22 with current family/pool context, week selector, family-member selector, game/pick controls, and clear empty/error states. Link it from the admin hub. Keep any JavaScript CSRF-protected; do not introduce CSRF-exempt JSON. Preserve user-owned template/CSS changes outside this admin page per D-23.</action>
  <verify>
    <automated>cd pickem &amp;&amp; ../venv/bin/python manage.py test pickem_homepage.tests.FamilyAdminExperienceTests --settings=pickem.test_settings --verbosity=2</automated>
    <manual>As owner/admin, load the manual picks page, select a family member/week/game, submit a pick, and confirm the pick appears only for the current pool.</manual>
  </verify>
  <done>Manual pick UI is usable, tested, CSRF-protected, and tenant-scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/JSON POST -> manual pick handler | Untrusted user, game, week, and pick inputs request score-affecting mutations. |
| manual pick handler -> GamePicks | Pool-scoped pick records can be created or overwritten. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12 | Tampering | manual pick POST | high | mitigate | Server-derive tenant/user/game metadata and ignore forged body fields. |
| T-05-13 | Information Disclosure | user-pick retrieval | high | mitigate | Resolve target user through active current-family membership and filter picks by pool. |
| T-05-14 | Repudiation | manual pick mutation | medium | mitigate | Audit every successful update with before/after pick metadata. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are planned; if introduced, stop for package legitimacy review. |
</threat_model>

<verification>
Focused tests, Django check, `git diff --check`, and manual owner/admin pick QA. Rollback: revert admin pick routes/forms/template; no migrations.
</verification>

<success_criteria>
Tenant admins can retrieve and submit user picks only inside the current family/pool, with direct negative tests for forged IDs and bodies.
</success_criteria>

<output>
Create `.planning/phases/05-family-admin-experience/05-05-SUMMARY.md` when done.
</output>
