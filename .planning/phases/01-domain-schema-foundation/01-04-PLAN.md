---
phase: 01-domain-schema-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified: []
autonomous: true
requirements:
  - TEN-02
  - TEN-03
  - TEN-04
  - TEN-05
  - POOL-01
  - POOL-02
  - COMM-01
  - COMM-03
  - SEC-01
must_haves:
  truths:
    - "All Phase 1 schema, migration, admin, and test artifacts from Plans 01, 02, and 03 exist before final verification runs."
    - "TEN-05: Legacy family, pool, pool settings, competition backfill, community backfill, and membership mapping are verified together."
    - "D-13/D-14: Owner/admin/member mappings include active superusers, active commissioners, active competition users, and active message-board-only users without downgrading existing owner/admin roles."
    - "D-11/D-22: Tenant FKs remain nullable and strict tenant-scoped uniqueness is still deferred."
    - "No Phase 1 verification task changes application code."
  prohibitions:
    - "Do not edit application code, migrations, templates, CSS, JavaScript, or runtime settings in this plan."
    - "Do not generate new migrations in this plan; pending migrations are a failure to report."
    - "Do not reinterpret deferred route, onboarding, cron, authorization, or UI work as Phase 1 scope."
  artifacts:
    - "pickem/pickem_api/migrations/0073_domain_schema_foundation.py"
    - "pickem/pickem_api/migrations/0074_add_legacy_pool_scope.py"
    - "pickem/pickem_homepage/migrations/0005_add_family_scope.py"
    - "pickem/pickem_api/tests.py"
    - "pickem/pickem_homepage/tests.py"
  key_links:
    - "Plan 01 core domain tables -> Plan 02 pool-scope migration"
    - "Plan 01 core domain tables -> Plan 03 family-scope migration"
    - "Plan 02 competition memberships + Plan 03 message-board memberships -> complete D-14 coverage"
    - "Plan 04 full-suite verification -> Phase 1 acceptance"
---

<objective>
Run final Phase 1 verification only after all implementation plans have completed.

Purpose: Full-suite verification must execute after both Wave 2 plans, because Plan 02 owns competition pool backfill and Plan 03 owns homepage family backfill.
Output: A verification summary proving Phase 1 artifacts are present, no migrations are pending, and the full Django check/test suite passes or records pre-existing failures.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-domain-schema-foundation/01-CONTEXT.md
@.planning/phases/01-domain-schema-foundation/01-RESEARCH.md
@.planning/phases/01-domain-schema-foundation/01-VALIDATION.md
@.planning/phases/01-domain-schema-foundation/01-01-SUMMARY.md
@.planning/phases/01-domain-schema-foundation/01-02-SUMMARY.md
@.planning/phases/01-domain-schema-foundation/01-03-SUMMARY.md
@MIGRATION_PLAN.md
@SECURITY_THREAT_MODEL.md
@TEST_PLAN.md
</context>

## Artifacts This Phase Produces

- Final Phase 1 verification evidence after Plans 01, 02, and 03 complete
- Confirmation that no application code is modified by this verification plan
- Summary of any pre-existing or newly introduced check/test failures

<tasks>

<task type="auto">
  <name>Task 1: Audit Phase 1 artifact and decision coverage</name>
  <files></files>
  <read_first>
    @.planning/phases/01-domain-schema-foundation/01-VALIDATION.md
    @.planning/phases/01-domain-schema-foundation/01-01-SUMMARY.md
    @.planning/phases/01-domain-schema-foundation/01-02-SUMMARY.md
    @.planning/phases/01-domain-schema-foundation/01-03-SUMMARY.md
  </read_first>
  <action>Inspect Phase 1 implementation summaries and resulting migration/test files without editing them. Confirm that D-01 through D-22 scoped to Phase 1 are represented by completed artifacts, especially D-13/D-14 membership mapping and the owner fallback chain from `01-VALIDATION.md`: no active superuser promotes the first active commissioner to owner, and no active superuser or active commissioner promotes the earliest active referenced competition or message-board user to owner. Confirm member backfill preserves existing owner/admin roles. Record any missing artifact or decision coverage in the summary as a blocker for phase acceptance.</action>
  <acceptance_criteria>All Phase 1 implementation summaries exist, required migrations/tests/admin changes are present, and the owner fallback plus role-preservation rules have executable test coverage across Plans 02 and 03.</acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-domain-schema-foundation/01-01-SUMMARY.md && test -f .planning/phases/01-domain-schema-foundation/01-02-SUMMARY.md && test -f .planning/phases/01-domain-schema-foundation/01-03-SUMMARY.md</automated>
  </verify>
  <done>Phase 1 artifacts are ready for full-suite verification, or blockers are explicitly recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Run final Phase 1 verification suite</name>
  <files></files>
  <read_first>
    @AGENTS.md
    @.planning/phases/01-domain-schema-foundation/01-VALIDATION.md
  </read_first>
  <action>Run the final Phase 1 verification commands after Plans 01, 02, and 03 are complete. Do not change application code or regenerate migrations in this plan. If `makemigrations --check --dry-run`, `check`, or tests fail, capture the exact failing command and the first actionable error in the summary, then stop and mark Phase 1 acceptance blocked.</action>
  <acceptance_criteria>Django reports no pending migrations, system checks pass, focused app tests pass, and the full test suite passes after both Wave 2 plans have completed.</acceptance_criteria>
  <verify>
    <automated>cd pickem && ../venv/bin/python manage.py makemigrations --check --dry-run --settings=pickem.test_settings</automated>
    <automated>cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings</automated>
    <automated>cd pickem && ../venv/bin/python manage.py test pickem_api --settings=pickem.test_settings --verbosity=2</automated>
    <automated>cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2</automated>
    <automated>cd pickem && ../venv/bin/python manage.py test --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>Final Phase 1 verification passes, or the summary records a concrete blocker with command output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Verification commands to local database | Test and migration checks exercise generated schema and migrations after all implementation plans. |
| Planning executor to application tree | This plan inspects and verifies application artifacts but must not modify application code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-16 | Repudiation | Phase 1 acceptance evidence | medium | mitigate | Run final verification after Plans 02 and 03, record exact command results in `01-04-SUMMARY.md`, and block acceptance on missing summaries or failing checks. |
| T-01-17 | Tampering | Verification-only plan scope | medium | mitigate | Keep `files_modified` empty and explicitly forbid application code, migration, template, CSS, JavaScript, and settings edits in this plan. |
| T-01-18 | Elevation of Privilege | Membership fallback validation | high | mitigate | Verify tests cover owner fallback and role preservation so the legacy family cannot be left ownerless and existing elevated roles are not downgraded. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No external packages are installed in Phase 1; package legitimacy audit says none. |
</threat_model>

<verification>
Run after Plans 01, 02, and 03 are complete:
- `cd pickem && ../venv/bin/python manage.py makemigrations --check --dry-run --settings=pickem.test_settings`
- `cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings`
- `cd pickem && ../venv/bin/python manage.py test pickem_api --settings=pickem.test_settings --verbosity=2`
- `cd pickem && ../venv/bin/python manage.py test pickem_homepage --settings=pickem.test_settings --verbosity=2`
- `cd pickem && ../venv/bin/python manage.py test --settings=pickem.test_settings --verbosity=2`
</verification>

<success_criteria>
Phase 1 is accepted only after all implementation plans complete and the final Wave 3 verification commands pass or document a concrete pre-existing blocker.
</success_criteria>

<output>
Create `.planning/phases/01-domain-schema-foundation/01-04-SUMMARY.md` when done.
</output>
