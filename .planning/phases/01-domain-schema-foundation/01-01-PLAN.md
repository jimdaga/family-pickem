---
phase: 01-domain-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pickem/pickem_api/models.py
  - pickem/pickem_api/admin.py
  - pickem/pickem_api/tests.py
  - pickem/pickem_api/migrations/0073_domain_schema_foundation.py
autonomous: true
requirements:
  - TEN-02
  - TEN-03
  - TEN-04
  - SEC-01
must_haves:
  truths:
    - "D-01: Family and Pool are separate Django models."
    - "D-02: Pool supports one default pool per family now and multiple pools later through Family+slug uniqueness."
    - "D-03/D-04: FamilyMembership stores owner/admin/member role and active/inactive status."
    - "D-05: PoolSettings exists with conservative fields only."
    - "D-15/D-16: FamilyInvitation stores code_hash, not raw invite codes."
    - "D-17/D-18: FamilyAuditLog exists for later sensitive action wiring."
  prohibitions:
    - "Do not add family or pool fields to GamesAndScores, GameWeeks, or Teams per D-06/D-07."
    - "Do not implement onboarding, tenant-aware routes, UI switchers, API route migration, or cron conversion."
    - "Do not install external packages."
  artifacts:
    - "pickem_api.Family"
    - "pickem_api.FamilyMembership"
    - "pickem_api.Pool"
    - "pickem_api.PoolSettings"
    - "pickem_api.FamilyInvitation"
    - "pickem_api.FamilyAuditLog"
    - "pickem/pickem_api/migrations/0073_domain_schema_foundation.py"
  key_links:
    - "FamilyMembership.user -> auth.User"
    - "Pool.family -> Family"
    - "PoolSettings.pool -> Pool"
    - "FamilyInvitation.family -> Family and optional pool -> Pool"
    - "FamilyAuditLog.family -> Family and optional pool -> Pool"
---

<objective>
Create the core tenant domain schema for Phase 1 without changing runtime route behavior.

Purpose: Phase 1 needs durable family, membership, pool, invitation, settings, and audit tables before any later authorization or route migration can safely scope data.
Output: New `pickem_api` model classes, generated schema migration, admin registrations, and focused model tests covering TEN-02, TEN-03, TEN-04, and SEC-01.
</objective>

<execution_context>
@/Users/jim/.codex/gsd-core/workflows/execute-plan.md
@/Users/jim/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-domain-schema-foundation/01-CONTEXT.md
@.planning/phases/01-domain-schema-foundation/01-RESEARCH.md
@.planning/phases/01-domain-schema-foundation/01-PATTERNS.md
@DISCOVERY.md
@FAMILY_MULTI_TENANCY_PLAN.md
@SECURITY_THREAT_MODEL.md
@MIGRATION_PLAN.md
@TEST_PLAN.md
@AGENTS.md
@pickem/pickem_api/models.py
@pickem/pickem_api/admin.py
@pickem/pickem_api/tests.py
@pickem/pickem_api/migrations/0072_rename_statustype_column.py
</context>

## Artifacts This Phase Produces

- `Family` model in `pickem/pickem_api/models.py`
- `FamilyMembership` model in `pickem/pickem_api/models.py`
- `Pool` model in `pickem/pickem_api/models.py`
- `PoolSettings` model in `pickem/pickem_api/models.py`
- `FamilyInvitation` model in `pickem/pickem_api/models.py`
- `FamilyAuditLog` model in `pickem/pickem_api/models.py`
- Tenant FK fields on legacy competition tables in Plan 02
- Family FK fields on homepage community/banner tables in Plan 03
- API migration `0073_domain_schema_foundation.py`
- API migration `0074_add_legacy_pool_scope.py` from Plan 02
- Homepage migration `0005_add_family_scope.py` from Plan 03
- Admin classes for every new tenant model
- Focused tests in `pickem_api/tests.py` and `pickem_homepage/tests.py`

## Source Coverage Audit

| Source | ID | Requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | - | Introduce family/pool schema and attach legacy data without broad runtime behavior changes | 01-01, 01-02, 01-03 | COVERED |
| REQ | TEN-02 | A family has at least one pool for a season/competition | 01-01, 01-02 | COVERED |
| REQ | TEN-03 | A signed-in user can belong to one or more families | 01-01, 01-02 | COVERED |
| REQ | TEN-04 | Membership role owner/admin/member | 01-01, 01-02 | COVERED |
| REQ | TEN-05 | Existing global data assigned to default legacy family/pool | 01-02, 01-03 | COVERED |
| REQ | POOL-01 | Picks scoped to a pool | 01-02 | COVERED |
| REQ | POOL-02 | Standings and weekly winners scoped to a pool | 01-02 | COVERED |
| REQ | COMM-01 | Message board scoped to family or pool | 01-03 | COVERED |
| REQ | COMM-03 | Banners do not leak across families | 01-03 | COVERED |
| REQ | SEC-01 | Security-sensitive admin actions are audit logged | 01-01 | COVERED |
| CONTEXT | D-01..D-05 | Tenant domain model decisions | 01-01 | COVERED |
| CONTEXT | D-06..D-08 | Global NFL reference data and pool-scoped picks/standings | 01-01, 01-02 | COVERED |
| CONTEXT | D-09..D-14 | Legacy backfill and membership mapping | 01-02, 01-03 | COVERED |
| CONTEXT | D-15..D-18 | Invitations and audit log storage | 01-01 | COVERED |
| CONTEXT | D-19..D-22 | Compatibility, nullable first, safe constraints | 01-01, 01-02, 01-03 | COVERED |
| RESEARCH | - | Additive Django migrations, no package installs, defer strict legacy uniqueness | 01-01, 01-02, 01-03 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tenant domain model tests first</name>
  <files>pickem/pickem_api/tests.py</files>
  <read_first>
    @pickem/pickem_api/tests.py
    @.planning/phases/01-domain-schema-foundation/01-CONTEXT.md
    @TEST_PLAN.md
  </read_first>
  <behavior>
    - Creating a `Family` with a unique slug works and its string representation is readable.
    - Creating multiple `Pool` rows for one family is possible when slugs differ; duplicate `(family, slug)` is rejected.
    - Creating `FamilyMembership` for one user in multiple families works; duplicate `(family, user)` is rejected; owner/admin/member and active/inactive choices are represented.
    - Creating `PoolSettings` for a pool works and duplicate settings for the same pool are rejected.
    - `FamilyInvitation` stores only `code_hash`, supports expiry/revocation/max-use fields, and no test or fixture expects a raw code field.
    - `FamilyAuditLog` can record family, optional pool, actor, action, target metadata, IP, user agent, and timestamp.
  </behavior>
  <action>Add failing tests to `pickem_api/tests.py` for the new tenant domain models per D-01 through D-05 and D-15 through D-18. Use existing `TestCase` style and `django.contrib.auth.models.User`. Include database constraint assertions with `IntegrityError` where appropriate. Do not modify serializers or views.</action>
  <acceptance_criteria>The tests describe TEN-02, TEN-03, TEN-04, and SEC-01 behavior before implementation and fail only because the model symbols or constraints do not exist yet.</acceptance_criteria>
  <verify>
    <automated>cd pickem && ../venv/bin/python manage.py test pickem_api --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>`pickem_api/tests.py` contains focused tenant domain tests and no route/UI behavior expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement core family, pool, invitation, and audit models</name>
  <files>pickem/pickem_api/models.py, pickem/pickem_api/migrations/0073_domain_schema_foundation.py</files>
  <read_first>
    @pickem/pickem_api/models.py
    @pickem/pickem_api/migrations/0072_rename_statustype_column.py
    @.planning/phases/01-domain-schema-foundation/01-PATTERNS.md
    @SECURITY_THREAT_MODEL.md
  </read_first>
  <action>Add `Family`, `FamilyMembership`, `Pool`, `PoolSettings`, `FamilyInvitation`, and `FamilyAuditLog` to `pickem_api.models` per D-01, D-02, D-03, D-04, D-05, D-15, D-16, D-17, and D-18. Use existing model style with timestamps, readable `__str__`, `verbose_name`, `verbose_name_plural`, and deterministic ordering. Add safe new-table constraints: unique `Family.slug`, unique `FamilyMembership(family, user)`, unique `Pool(family, slug)`, one `PoolSettings` per pool, unique `FamilyInvitation.code_hash`, role/status/action choice constraints where Django generates portable constraints. Add indexes for `Family.slug`, membership lookup by `(family, user, status)`, pool lookup by `(family, slug, status)`, invitation lookup by `code_hash`, and audit lookup by `(family, created_at)`. Use `on_delete=PROTECT` or `SET_NULL` so deleting tenant containers does not silently destroy historical audit rows. Do not add tenant fields to `GamesAndScores`, `GameWeeks`, or `Teams` per D-06/D-07. Generate `0073_domain_schema_foundation.py` with dependency on `pickem_api.0072_rename_statustype_column` and `settings.AUTH_USER_MODEL`; no data backfill belongs in this migration.</action>
  <acceptance_criteria>The new domain schema exists, preserves global NFL reference models unchanged, uses no external packages, and only adds safe constraints on new tables.</acceptance_criteria>
  <verify>
    <automated>cd pickem && ../venv/bin/python manage.py makemigrations pickem_api --settings=pickem.test_settings</automated>
    <automated>cd pickem && ../venv/bin/python manage.py makemigrations --check --dry-run --settings=pickem.test_settings</automated>
    <automated>cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings</automated>
    <automated>cd pickem && ../venv/bin/python manage.py test pickem_api --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>`0073_domain_schema_foundation.py` creates only new tenant domain tables and the domain model tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Register tenant domain models in Django admin</name>
  <files>pickem/pickem_api/admin.py, pickem/pickem_api/tests.py</files>
  <read_first>
    @pickem/pickem_api/admin.py
    @pickem/pickem_api/tests.py
    @.planning/phases/01-domain-schema-foundation/01-PATTERNS.md
  </read_first>
  <action>Register `Family`, `FamilyMembership`, `Pool`, `PoolSettings`, `FamilyInvitation`, and `FamilyAuditLog` in `pickem_api/admin.py` per D-17/D-18. Use existing `ModelAdmin` patterns: `list_display`, `list_filter`, `search_fields`, ordering, `date_hierarchy` where useful, and readonly timestamps. For `FamilyInvitationAdmin`, display `code_hash` but never any raw invite code. For `FamilyAuditLogAdmin`, make audit timestamp and metadata fields readonly. Add a lightweight test in `pickem_api/tests.py` that the new model classes are registered with `admin.site`.</action>
  <acceptance_criteria>Admin users can inspect the new schema and audit rows without exposing raw invite secrets or changing current commissioner routes.</acceptance_criteria>
  <verify>
    <automated>cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings</automated>
    <automated>cd pickem && ../venv/bin/python manage.py test pickem_api --settings=pickem.test_settings --verbosity=2</automated>
  </verify>
  <done>All new tenant domain models are registered in admin and invite/audit admin behavior follows secrecy and immutability expectations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Django app to database | New tenant role, invite, settings, and audit data becomes authoritative for later access control. |
| Admin user to Django admin | Staff users can inspect tenant containers, memberships, invitations, and audit logs. |
| Future invite flow to database | Phase 1 stores invite metadata before public join flows exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Spoofing | `FamilyMembership.role` | high | mitigate | Constrain roles to owner/admin/member per D-03 and add tests for role choices. |
| T-01-02 | Tampering | `FamilyMembership` duplicates | high | mitigate | Enforce unique `(family, user)` and status field per D-04 so stale duplicate role rows cannot grant access later. |
| T-01-03 | Information Disclosure | `FamilyInvitation` | high | mitigate | Store only `code_hash` per D-16; admin and tests must not introduce a raw invite-code field. |
| T-01-04 | Repudiation | `FamilyAuditLog` | high | mitigate | Add append-oriented audit model with actor, action, target, metadata, IP, user agent, and timestamp per D-17. |
| T-01-05 | Denial of Service | New-table constraints | medium | mitigate | Apply constraints only to new tables in this plan; no strict uniqueness on legacy tables per D-22. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No external packages are installed in Phase 1; package legitimacy audit says none. |
</threat_model>

<verification>
Run after this plan:
- `cd pickem && ../venv/bin/python manage.py makemigrations --check --dry-run --settings=pickem.test_settings`
- `cd pickem && ../venv/bin/python manage.py check --settings=pickem.test_settings`
- `cd pickem && ../venv/bin/python manage.py test pickem_api --settings=pickem.test_settings --verbosity=2`
</verification>

<success_criteria>
The core tenant domain schema is present, tested, admin-visible, and does not alter existing global NFL reference tables or route behavior.
</success_criteria>

<output>
Create `.planning/phases/01-domain-schema-foundation/01-01-SUMMARY.md` when done.
</output>
