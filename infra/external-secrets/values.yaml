# External Secrets Operator Helm chart values (v0.19.x)
# Managed by ArgoCD
# Note: Pinned to 0.19.x due to K8s 1.28 compatibility (selectableFields CRD field)

installCRDs: true

crds:
  createClusterExternalSecret: true
  createClusterSecretStore: true
  createPushSecret: true

# Single-node: minimal resources
replicaCount: 1

webhook:
  replicaCount: 1

certController:
  replicaCount: 1
