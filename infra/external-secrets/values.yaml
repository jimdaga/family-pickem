# External Secrets Operator v2 Helm chart values
# Managed by ArgoCD

# CRDs are managed by the chart
crds:
  createClusterExternalSecret: true
  createClusterSecretStore: true
  createPushSecret: true

# Single-node: minimal resources
replicaCount: 1

webhook:
  replicaCount: 1

certController:
  replicaCount: 1
